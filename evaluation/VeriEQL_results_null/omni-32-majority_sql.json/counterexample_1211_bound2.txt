-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE,
	ACL_IGG INTEGER,
	ACL_IGM INTEGER,
	ANA INTEGER,
	ANA_PATTERN VARCHAR(20),
	ACL_IGA INTEGER,
	DIAGNOSIS VARCHAR(20),
	KCT VARCHAR(20),
	RVVT VARCHAR(20),
	LAC VARCHAR(20),
	SYMPTOMS VARCHAR(20),
	THROMBOSIS INTEGER
);
INSERT INTO EXAMINATION VALUES (-1, '1000-01-01', 0, 0, 0, '', 0, '', '', '', '', '', 0);
INSERT INTO EXAMINATION VALUES (0, '1000-01-01', 0, 0, 0, '', 0, '', '', '', '', '', 0);
CREATE TABLE PATIENT (
	ID INTEGER,
	SEX VARCHAR(20),
	BIRTHDAY DATE,
	DESCRIPTION DATE,
	FIRST_DATE DATE,
	ADMISSION VARCHAR(20),
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (-1, '', '8000-02-01', '1000-03-01', '1000-09-01', '', '');
INSERT INTO PATIENT VALUES (0, '', '1000-02-01', '1000-01-01', '1000-01-01', '', '');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	GOT INTEGER,
	GPT INTEGER,
	LDH INTEGER,
	ALP INTEGER,
	TP INTEGER,
	ALB INTEGER,
	UA INTEGER,
	UN INTEGER,
	CRE INTEGER,
	T_BIL INTEGER,
	T_CHO INTEGER,
	TG INTEGER,
	CPK INTEGER,
	GLU INTEGER,
	WBC INTEGER,
	RBC INTEGER,
	HGB INTEGER,
	HCT INTEGER,
	PLT INTEGER,
	PT INTEGER,
	APTT INTEGER,
	FG INTEGER,
	PIC INTEGER,
	TAT INTEGER,
	TAT2 INTEGER,
	U_PRO VARCHAR(20),
	IGG INTEGER,
	IGA INTEGER,
	IGM INTEGER,
	CRP VARCHAR(20),
	RA VARCHAR(20),
	RF VARCHAR(20),
	C3 INTEGER,
	C4 INTEGER,
	RNP VARCHAR(20),
	SM VARCHAR(20),
	SC170 VARCHAR(20),
	SSA VARCHAR(20),
	SSB VARCHAR(20),
	CENTROMEA VARCHAR(20),
	DNA VARCHAR(20),
	DNA_II INTEGER
);
INSERT INTO LABORATORY VALUES (0, '1000-12-01', 0, 0, 599, 0, 45, 46, 47, 0, 48, 49, 0, 0, 0, 0, 50, 51, 52, 53, 0, 54, 0, 55, 0, 0, 0, '', 0, 0, 0, '', '', '', 0, 0, '', '', '', '', '', '', '', 0);
INSERT INTO LABORATORY VALUES (-1, '1000-12-30', 0, 0, 600, 0, 56, 57, 58, 0, 60, 61, 0, 0, 0, 0, 62, 63, 64, 65, 0, 66, 0, 67, 0, 0, 0, '', 0, 0, 0, '', '', '', 0, 0, '', '', '', '', '', '', '', 0);
-- ----------sql1------------
-- -1, NULL
SELECT P.ID, STRFTIME('%Y', CURRENT_TIMESTAMP) - STRFTIME('%Y', P.BIRTHDAY) AS AGE FROM LABORATORY AS L INNER JOIN PATIENT AS P ON L.ID = P.ID WHERE L.LDH BETWEEN 600 AND 800;
-- ----------sql2------------
SELECT DISTINCT T1.ID, STRFTIME('%Y', CURRENT_TIMESTAMP) - STRFTIME('%Y', T1.BIRTHDAY) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.LDH > 600 AND T2.LDH < 800;
