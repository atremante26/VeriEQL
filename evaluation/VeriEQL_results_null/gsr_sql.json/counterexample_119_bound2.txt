-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, 0, '', '1993-09-01');
INSERT INTO ACCOUNT VALUES (-1, 0, 'POPLATEK PO OBRATU', '1993-05-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20),
	ISSUED DATE
);
INSERT INTO CARD VALUES (0, 0, '', '1000-08-01');
INSERT INTO CARD VALUES (1, -1, '', '1000-02-01');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	GENDER VARCHAR(20),
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (1, 'D', '1000-06-01', 1);
INSERT INTO CLIENT VALUES (0, '', '1000-01-01', 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (0, 0, 0, '');
INSERT INTO DISP VALUES (-1, 1, 0, 'Z');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A2 VARCHAR(20),
	A3 VARCHAR(20),
	A4 VARCHAR(20),
	A5 VARCHAR(20),
	A6 VARCHAR(20),
	A7 VARCHAR(20),
	A8 INTEGER,
	A9 INTEGER,
	A10 INTEGER,
	A11 INTEGER,
	A12 INTEGER,
	A13 INTEGER,
	A14 INTEGER,
	A15 INTEGER,
	A16 INTEGER
);
INSERT INTO DISTRICT VALUES (0, NULL, '^_', NULL, NULL, 'W', 'DD', 0, 0, NULL, NULL, 43, 44, 0, 0, NULL);
INSERT INTO DISTRICT VALUES (1, 'DD', '[', 'DD', '', '', NULL, 0, NULL, 45, 0, NULL, 47, 0, 0, NULL);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER,
	PAYMENTS INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (0, 0, '1000-07-01', 0, 0, 0, 'D');
INSERT INTO LOAN VALUES (-1, -1, '1000-05-01', 0, 0, 0, '');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	BANK_TO VARCHAR(20),
	ACCOUNT_TO INTEGER,
	AMOUNT INTEGER,
	K_SYMBOL VARCHAR(20)
);
INSERT INTO ORDER VALUES (-1, 0, '', 0, 0, '');
INSERT INTO ORDER VALUES (0, -1, 'D', 0, 0, '');
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	TYPE VARCHAR(20),
	OPERATION VARCHAR(20),
	AMOUNT INTEGER,
	BALANCE INTEGER,
	K_SYMBOL VARCHAR(20),
	BANK VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, '1000-12-01', 'D', '', 0, 0, '', '', 0);
INSERT INTO TRANS VALUES (1, 0, '1000-03-01', 'D', '', 0, 0, '', 'D', 0);
-- ----------sql1------------
SELECT A.ACCOUNT_ID, D.A2 AS DISTRICT_NAME, D.A3 AS REGION FROM ACCOUNT A JOIN DISP DP ON A.ACCOUNT_ID = DP.ACCOUNT_ID JOIN DISTRICT D ON A.DISTRICT_ID = D.DISTRICT_ID WHERE A.DATE LIKE '1993%' AND A.FREQUENCY = 'POPLATEK PO OBRATU';
-- ----------sql2------------
-- -1, NULL, ^_
SELECT T1.ACCOUNT_ID, T2.A2, T2.A3 FROM ACCOUNT AS T1 INNER JOIN DISTRICT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE T1.FREQUENCY = 'POPLATEK PO OBRATU' AND STRFTIME('%Y', T1.DATE)= '1993';
