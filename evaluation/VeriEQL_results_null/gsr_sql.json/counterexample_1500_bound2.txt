-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CUSTOMERS (
	CUSTOMERID INTEGER,
	SEGMENT VARCHAR(20),
	CURRENCY VARCHAR(20)
);
INSERT INTO CUSTOMERS VALUES (1, '', '');
INSERT INTO CUSTOMERS VALUES (0, '', '');
CREATE TABLE GASSTATIONS (
	GASSTATIONID INTEGER,
	CHAINID INTEGER,
	COUNTRY VARCHAR(20),
	SEGMENT VARCHAR(20)
);
INSERT INTO GASSTATIONS VALUES (-1, 0, '', 'V');
INSERT INTO GASSTATIONS VALUES (0, 0, 'W', 'X');
CREATE TABLE PRODUCTS (
	PRODUCTID INTEGER,
	DESCRIPTION VARCHAR(20)
);
INSERT INTO PRODUCTS VALUES (-1, 'K');
INSERT INTO PRODUCTS VALUES (0, 'O');
CREATE TABLE TRANSACTIONS_1K (
	TRANSACTIONID INTEGER,
	DATE DATE,
	TIME VARCHAR(20),
	CUSTOMERID INTEGER,
	CARDID INTEGER,
	GASSTATIONID INTEGER,
	PRODUCTID INTEGER,
	AMOUNT INTEGER,
	PRICE INTEGER
);
INSERT INTO TRANSACTIONS_1K VALUES (-1, '1000-03-01', '', 2, 0, 0, 0, 0, 22);
INSERT INTO TRANSACTIONS_1K VALUES (0, '1000-01-01', '', 0, 0, 0, 0, 0, NULL);
CREATE TABLE YEARMONTH (
	CUSTOMERID INTEGER,
	DATE VARCHAR(20),
	CONSUMPTION INTEGER
);
INSERT INTO YEARMONTH VALUES (1, 'GAC', 24);
INSERT INTO YEARMONTH VALUES (0, '201309', 25);
-- ----------sql1------------
SELECT P.DESCRIPTION FROM TRANSACTIONS_1K T JOIN PRODUCTS P ON T.PRODUCTID = P.PRODUCTID WHERE STRFTIME('%Y%M', T.DATE) = '201309';
-- ----------sql2------------
-- O
SELECT T3.DESCRIPTION FROM TRANSACTIONS_1K AS T1 INNER JOIN YEARMONTH AS T2 ON T1.CUSTOMERID = T2.CUSTOMERID INNER JOIN PRODUCTS AS T3 ON T1.PRODUCTID = T3.PRODUCTID WHERE T2.DATE = '201309';
