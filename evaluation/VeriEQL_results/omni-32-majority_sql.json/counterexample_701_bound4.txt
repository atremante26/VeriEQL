-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-3, -1);
INSERT INTO BADGES VALUES (-2, 1);
INSERT INTO BADGES VALUES (-1, 1);
INSERT INTO BADGES VALUES (0, -1);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	SCORE INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-2, 3, 0, -1);
INSERT INTO COMMENTS VALUES (-1, 3, 0, -1);
INSERT INTO COMMENTS VALUES (0, 3, 0, -1);
INSERT INTO COMMENTS VALUES (1, 3, 0, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-3, 3, -1);
INSERT INTO POSTHISTORY VALUES (-2, 3, -1);
INSERT INTO POSTHISTORY VALUES (-1, 1, -1);
INSERT INTO POSTHISTORY VALUES (0, 3, -1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-2, 1, 3);
INSERT INTO POSTLINKS VALUES (-1, 3, 2);
INSERT INTO POSTLINKS VALUES (0, 3, 3);
INSERT INTO POSTLINKS VALUES (1, 3, 3);
CREATE TABLE POSTS (
	ID INTEGER,
	SCORE INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, 0, -1, 3);
INSERT INTO POSTS VALUES (2, 0, 0, -1, 3);
INSERT INTO POSTS VALUES (1, 0, 0, -1, 0);
INSERT INTO POSTS VALUES (3, 0, 0, -1, 3);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-2, 0, 3);
INSERT INTO TAGS VALUES (0, 0, 3);
INSERT INTO TAGS VALUES (-1, 0, 3);
INSERT INTO TAGS VALUES (1, 0, 3);
CREATE TABLE USERS (
	ID INTEGER,
	REPUTATION INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (0, NULL, 0);
INSERT INTO USERS VALUES (2, NULL, 0);
INSERT INTO USERS VALUES (1, NULL, 0);
INSERT INTO USERS VALUES (-1, NULL, 0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 3, -1);
INSERT INTO VOTES VALUES (-1, 3, -1);
INSERT INTO VOTES VALUES (0, 3, -1);
INSERT INTO VOTES VALUES (1, 3, -1);
-- ----------sql1------------
-- 0
SELECT CAST(SUM(CASE WHEN P.SCORE > 50 THEN 1 ELSE 0 END) AS REAL) * 100.0 / COUNT(P.ID) AS PERCENTAGE FROM POSTS P WHERE P.OWNERUSERID = ( SELECT U.ID FROM USERS U ORDER BY U.REPUTATION DESC LIMIT 1 );
-- ----------sql2------------
-- NULL
SELECT CAST(SUM(CASE WHEN T2.SCORE > 50 THEN 1 ELSE 0 END) AS REAL) * 100 / COUNT(T1.ID) FROM USERS T1 INNER JOIN POSTS T2 ON T1.ID = T2.OWNERUSERID INNER JOIN ( SELECT MAX(REPUTATION) AS MAX_REPUTATION FROM USERS ) T3 ON T1.REPUTATION = T3.MAX_REPUTATION;
