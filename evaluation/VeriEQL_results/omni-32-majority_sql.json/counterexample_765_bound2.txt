-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER
);
INSERT INTO ALIGNMENT VALUES (0);
INSERT INTO ALIGNMENT VALUES (1);
CREATE TABLE ATTRIBUTE (
	ID INTEGER
);
INSERT INTO ATTRIBUTE VALUES (1);
INSERT INTO ATTRIBUTE VALUES (0);
CREATE TABLE COLOUR (
	ID INTEGER
);
INSERT INTO COLOUR VALUES (0);
INSERT INTO COLOUR VALUES (-1);
CREATE TABLE GENDER (
	ID INTEGER
);
INSERT INTO GENDER VALUES (0);
INSERT INTO GENDER VALUES (1);
CREATE TABLE PUBLISHER (
	ID INTEGER
);
INSERT INTO PUBLISHER VALUES (0);
INSERT INTO PUBLISHER VALUES (-1);
CREATE TABLE RACE (
	ID INTEGER
);
INSERT INTO RACE VALUES (1);
INSERT INTO RACE VALUES (0);
CREATE TABLE SUPERHERO (
	ID INTEGER,
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER
);
INSERT INTO SUPERHERO VALUES (0, 1, 0, 0, -1, 1, -1, 1);
INSERT INTO SUPERHERO VALUES (-1, 1, 0, 0, -1, 1, -1, 1);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (-1, 1);
INSERT INTO HERO_ATTRIBUTE VALUES (0, 1);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (1, 'STEALTH');
INSERT INTO SUPERPOWER VALUES (0, 'STEALTH');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (0, 1);
INSERT INTO HERO_POWER VALUES (0, 1);
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT HP.HERO_ID) AS STEALTH_HERO_COUNT FROM SUPERPOWER AS SP INNER JOIN HERO_POWER AS HP ON SP.ID = HP.POWER_ID WHERE SP.POWER_NAME = 'STEALTH';
-- ----------sql2------------
-- 2
SELECT COUNT(T1.HERO_ID) FROM HERO_POWER AS T1 INNER JOIN SUPERPOWER AS T2 ON T1.POWER_ID = T2.ID WHERE T2.POWER_NAME = 'STEALTH';
