-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, 0, '1002-01-31');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (0, 0, '1000-03-01', -9, 25, '');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	AMOUNT INTEGER
);
INSERT INTO ORDER VALUES (0, 0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, '1000-12-01', 0, 0);
-- ----------sql1------------
SELECT A.ACCOUNT_ID FROM LOAN L INNER JOIN ACCOUNT A ON L.ACCOUNT_ID = A.ACCOUNT_ID WHERE L.DURATION > 24 AND L.STATUS = 'A' AND A.DATE < '1997-01-01' AND L.AMOUNT = ( SELECT MIN(AMOUNT) FROM LOAN WHERE DURATION > 24 AND STATUS = 'A' );
-- ----------sql2------------
-- 0
SELECT T1.ACCOUNT_ID FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T1.DURATION > 24 AND STRFTIME('%Y', T2.DATE) < '1997' ORDER BY T1.AMOUNT ASC LIMIT 1;
