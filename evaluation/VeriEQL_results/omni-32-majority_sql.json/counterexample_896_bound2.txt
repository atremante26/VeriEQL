-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO CIRCUITS VALUES (-1, '2147483648');
INSERT INTO CIRCUITS VALUES (0, '2147483648');
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO CONSTRUCTORS VALUES (-1, '2147483648');
INSERT INTO CONSTRUCTORS VALUES (0, '2147483648');
CREATE TABLE DRIVERS (
	DRIVERID INTEGER,
	SURNAME VARCHAR(20)
);
INSERT INTO DRIVERS VALUES (1, '2848918144155936697');
INSERT INTO DRIVERS VALUES (0, 'HAMILTON');
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (2011);
INSERT INTO SEASONS VALUES (2010);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO RACES VALUES (-1, 2011, 0, '2147483648');
INSERT INTO RACES VALUES (0, 2010, 0, '2147483648');
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (-1, 0, 0);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (1, 0, 0, 0);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, 0, -1, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	POSITION INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (0, -1, 1, 0);
INSERT INTO DRIVERSTANDINGS VALUES (1, -1, 1, 0);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER,
	POSITION INTEGER
);
INSERT INTO LAPTIMES VALUES (0, 1, 1, 0);
INSERT INTO LAPTIMES VALUES (-1, 0, 0, 0);
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER
);
INSERT INTO PITSTOPS VALUES (-1, 1, 0, 0);
INSERT INTO PITSTOPS VALUES (0, 0, 1, 0);
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER
);
INSERT INTO QUALIFYING VALUES (0, 0, 1, 0, 0);
INSERT INTO QUALIFYING VALUES (1, 0, 1, -1, 0);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (1);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER,
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (-1, -1, 0, -1, 0, 1);
INSERT INTO RESULTS VALUES (0, 0, 0, 0, 0, 1);
-- ----------sql1------------
-- 0
SELECT CAST(SUM(CASE WHEN RS.POSITION > 1 THEN 1 ELSE 0 END) AS REAL) * 100.0 / COUNT(RS.RACEID) AS PERCENTAGE FROM DRIVERS D INNER JOIN RESULTS RS ON D.DRIVERID = RS.DRIVERID INNER JOIN RACES R ON RS.RACEID = R.RACEID WHERE D.SURNAME = 'HAMILTON' AND R.YEAR >= 2010;
-- ----------sql2------------
-- NULL
SELECT CAST(COUNT(CASE WHEN T2.POSITION <> 1 THEN T2.POSITION END) AS REAL) * 100 / COUNT(T2.DRIVERSTANDINGSID) FROM RACES AS T1 INNER JOIN DRIVERSTANDINGS AS T2 ON T2.RACEID = T1.RACEID INNER JOIN DRIVERS AS T3 ON T3.DRIVERID = T2.DRIVERID WHERE T3.SURNAME = 'HAMILTON' AND T1.YEAR >= 2010;
