-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, -2, '1000-01-01');
INSERT INTO ACCOUNT VALUES (-1, -2, '1000-01-01');
INSERT INTO ACCOUNT VALUES (1, 0, '1000-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (-1, 0);
INSERT INTO CARD VALUES (0, 0);
INSERT INTO CARD VALUES (1, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (-2, NULL, -1);
INSERT INTO CLIENT VALUES (0, NULL, 0);
INSERT INTO CLIENT VALUES (-1, NULL, -2);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (-1, 0, 0);
INSERT INTO DISP VALUES (1, 0, 0);
INSERT INTO DISP VALUES (0, -2, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A11 INTEGER
);
INSERT INTO DISTRICT VALUES (-1, NULL);
INSERT INTO DISTRICT VALUES (0, NULL);
INSERT INTO DISTRICT VALUES (-2, NULL);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE
);
INSERT INTO LOAN VALUES (0, -1, '1000-01-01');
INSERT INTO LOAN VALUES (2, 1, '1000-01-01');
INSERT INTO LOAN VALUES (1, 0, '1000-01-01');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (-1, -1);
INSERT INTO ORDER VALUES (1, 0);
INSERT INTO ORDER VALUES (0, 1);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (2, -1, '1000-01-01', 0);
INSERT INTO TRANS VALUES (0, -1, '1000-01-01', 0);
INSERT INTO TRANS VALUES (1, 1, '1000-01-01', 0);
-- ----------sql1------------
SELECT DISTINCT A.ACCOUNT_ID FROM ACCOUNT A INNER JOIN DISP D ON A.ACCOUNT_ID = D.ACCOUNT_ID INNER JOIN CLIENT C ON D.CLIENT_ID = C.CLIENT_ID INNER JOIN DISTRICT DI ON C.DISTRICT_ID = DI.DISTRICT_ID WHERE C.BIRTH_DATE = ( SELECT MAX(BIRTH_DATE) FROM CLIENT ) AND DI.A11 = ( SELECT MAX(A11) FROM DISTRICT );
-- ----------sql2------------
-- 0
SELECT T1.ACCOUNT_ID  FROM ACCOUNT AS T1 INNER JOIN DISP AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID INNER JOIN CLIENT AS T3 ON T2.CLIENT_ID = T3.CLIENT_ID INNER JOIN DISTRICT AS T4 ON T4.DISTRICT_ID = T1.DISTRICT_ID WHERE T2.CLIENT_ID = ( SELECT CLIENT_ID FROM CLIENT ORDER BY BIRTH_DATE DESC LIMIT 1) GROUP BY T4.A11, T1.ACCOUNT_ID;
