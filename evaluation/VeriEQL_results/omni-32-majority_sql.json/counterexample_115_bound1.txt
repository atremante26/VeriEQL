-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (0, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	GENDER VARCHAR(20),
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, '6404956032923022035', 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A3 VARCHAR(20),
	A4 VARCHAR(20)
);
INSERT INTO DISTRICT VALUES (0, 'SOUTH BOHEMIA', NULL);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (0, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO TRANS VALUES (0, 0);
-- ----------sql1------------
-- NULL
SELECT (CAST(COUNT(CASE WHEN CLIENT.GENDER = 'M' THEN 1 END) AS REAL) * 100) / COUNT(*) AS PERCENTAGE_MALE_CLIENTS FROM CLIENT INNER JOIN DISTRICT ON CLIENT.DISTRICT_ID = DISTRICT.DISTRICT_ID WHERE DISTRICT.A3 = 'SOUTH BOHEMIA' AND DISTRICT.A4 = (SELECT MAX(A4) FROM DISTRICT WHERE A3 = 'SOUTH BOHEMIA');
-- ----------sql2------------
-- 0
SELECT CAST(SUM(T1.GENDER = 'M') AS REAL) * 100 / COUNT(T1.CLIENT_ID) FROM CLIENT AS T1 INNER JOIN DISTRICT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE T2.A3 = 'SOUTH BOHEMIA' GROUP BY T2.A4 ORDER BY T2.A4 DESC LIMIT 1;
