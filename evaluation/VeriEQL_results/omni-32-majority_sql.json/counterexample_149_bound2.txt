-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (-1, 1);
INSERT INTO ACCOUNT VALUES (0, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO CARD VALUES (0, 0, '2147483648');
INSERT INTO CARD VALUES (1, 0, '2147483648');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (1, 1);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (0, 1, -1, '4364736977825917429');
INSERT INTO DISP VALUES (1, 1, 0, '4364736977825917431');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A11 INTEGER
);
INSERT INTO DISTRICT VALUES (1, 8001);
INSERT INTO DISTRICT VALUES (0, 8000);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (0, 0);
INSERT INTO LOAN VALUES (-1, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
INSERT INTO ORDER VALUES (-1, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (-1, 0, '2147483648', 0);
INSERT INTO TRANS VALUES (0, -1, '2147483648', 0);
-- ----------sql1------------
-- 4364736977825917429
SELECT DISTINCT DISP.TYPE FROM DISTRICT INNER JOIN ACCOUNT ON DISTRICT.DISTRICT_ID = ACCOUNT.DISTRICT_ID INNER JOIN DISP ON ACCOUNT.ACCOUNT_ID = DISP.ACCOUNT_ID WHERE DISP.TYPE != 'OWNER' AND DISTRICT.A11 > 8000 AND DISTRICT.A11 <= 9000;
-- ----------sql2------------
-- 4364736977825917429
-- 4364736977825917431
SELECT T3.TYPE FROM DISTRICT AS T1 INNER JOIN ACCOUNT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID INNER JOIN DISP AS T3 ON T2.ACCOUNT_ID = T3.ACCOUNT_ID WHERE T3.TYPE != 'OWNER' AND T1.A11 BETWEEN 8000 AND 9000;
