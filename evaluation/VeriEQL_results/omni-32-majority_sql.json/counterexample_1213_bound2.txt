-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (-1);
INSERT INTO EXAMINATION VALUES (-1);
CREATE TABLE PATIENT (
	ID INTEGER,
	BIRTHDAY DATE
);
INSERT INTO PATIENT VALUES (-1, '1982-04-01');
INSERT INTO PATIENT VALUES (0, '1982-04-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	ALP INTEGER,
	TAT INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '1000-01-01', 300, 0, '2147483648');
INSERT INTO LABORATORY VALUES (0, '1000-01-02', 300, 0, '2147483648');
-- ----------sql1------------
-- -1, OUTSIDE NORMAL RANGE
-- 0, OUTSIDE NORMAL RANGE
SELECT P.ID, CASE WHEN L.ALP < 300 THEN 'WITHIN NORMAL RANGE' ELSE 'OUTSIDE NORMAL RANGE' END AS ALP_STATUS FROM PATIENT P INNER JOIN LABORATORY L ON P.ID = L.ID WHERE P.BIRTHDAY = '1982-04-01';
-- ----------sql2------------
-- -1, ABNORMAL
-- 0, ABNORMAL
SELECT T1.ID , CASE WHEN T2.ALP < 300 THEN 'NORMAL' ELSE 'ABNORMAL' END FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T1.BIRTHDAY = '1982-04-01';
