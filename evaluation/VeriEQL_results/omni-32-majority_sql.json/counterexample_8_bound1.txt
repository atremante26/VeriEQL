-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20),
	FRPM_COUNT_K12 INTEGER
);
INSERT INTO FRPM VALUES ('2147483648', NULL);
CREATE TABLE SATSCORES (
	CDS VARCHAR(20),
	NUMTSTTAKR INTEGER
);
INSERT INTO SATSCORES VALUES ('2147483648', 0);
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('2147483648');
-- ----------sql1------------
SELECT SATSCORES.NUMTSTTAKR FROM FRPM INNER JOIN SATSCORES ON FRPM.CDSCODE = SATSCORES.CDS WHERE FRPM.FRPM_COUNT_K12 = ( SELECT MAX(FRPM_COUNT_K12) FROM FRPM );
-- ----------sql2------------
-- 0
SELECT NUMTSTTAKR FROM SATSCORES WHERE CDS = ( SELECT CDSCODE FROM FRPM ORDER BY FRPM_COUNT_K12 DESC LIMIT 1 );
