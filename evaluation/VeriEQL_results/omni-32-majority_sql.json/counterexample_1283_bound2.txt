-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (-1);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER,
	SEX VARCHAR(20)
);
INSERT INTO PATIENT VALUES (-1, 'M');
INSERT INTO PATIENT VALUES (0, 'M');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	GOT INTEGER,
	GPT INTEGER,
	UN INTEGER,
	PT INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '1000-01-01', 60, 0, 0, 14, '2147483648');
INSERT INTO LABORATORY VALUES (0, '1000-01-02', 60, 0, 0, 15, '2147483648');
-- ----------sql1------------
-- 2
SELECT COUNT(*) AS MALEPATIENTSWITHNORMALGPT FROM PATIENT AS P INNER JOIN LABORATORY AS L ON P.ID = L.ID WHERE P.SEX = 'M' AND L.GPT < 60;
-- ----------sql2------------
-- 0
SELECT COUNT(T1.ID) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.GOT < 60 AND T1.SEX = 'M';
