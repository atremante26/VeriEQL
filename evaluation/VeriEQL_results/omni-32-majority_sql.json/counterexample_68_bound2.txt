-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('[');
INSERT INTO FRPM VALUES ('');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('[');
INSERT INTO SATSCORES VALUES ('');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20),
	CLOSEDDATE DATE,
	SOC VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('[', 'CLOSED', ']', NULL, '1989-01-01', '11');
INSERT INTO SCHOOLS VALUES ('', 'CLOSED', '', '', '1988-02-01', '11');
-- ----------sql1------------
-- ]
SELECT COUNTY FROM SCHOOLS WHERE SOC = 11 AND STATUSTYPE = 'CLOSED' AND STRFTIME('%Y', CLOSEDDATE) BETWEEN '1980' AND '1989' GROUP BY COUNTY ORDER BY COUNT(*) DESC LIMIT 1;
-- ----------sql2------------
-- 
SELECT COUNTY FROM SCHOOLS WHERE STRFTIME('%Y', CLOSEDDATE) BETWEEN '1980' AND '1989' AND STATUSTYPE = 'CLOSED' AND SOC = 11 GROUP BY COUNTY ORDER BY COUNT(SCHOOL) DESC LIMIT 1;
