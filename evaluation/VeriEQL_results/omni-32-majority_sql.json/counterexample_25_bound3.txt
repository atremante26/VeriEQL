-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20),
	DISTRICT_NAME VARCHAR(20),
	CHARTER_FUNDING_TYPE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('\u{157}', 'RIVERSIDE', '');
INSERT INTO FRPM VALUES ('\u{127}', '\u{10d}', '');
INSERT INTO FRPM VALUES ('', '', '');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20),
	SNAME VARCHAR(20),
	AVGSCRMATH INTEGER
);
INSERT INTO SATSCORES VALUES ('\u{157}', 'q', 401);
INSERT INTO SATSCORES VALUES ('\u{127}', '', 1);
INSERT INTO SATSCORES VALUES ('', '', 1);
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	COUNTY VARCHAR(20),
	DISTRICT VARCHAR(20),
	SCHOOL VARCHAR(20),
	CHARTER INTEGER,
	FUNDINGTYPE VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('\u{157}', '', '', '', -2147483648, '');
INSERT INTO SCHOOLS VALUES ('', 'e', '', '', -2147483648, '');
INSERT INTO SCHOOLS VALUES ('\u{127}', 'vwxuy', '', '', -2147483648, '');
-- ----------sql1------------
SELECT T1.SCHOOL, T1.FUNDINGTYPE FROM SCHOOLS AS T1 INNER JOIN SATSCORES AS T2 ON T1.CDSCODE = T2.CDS WHERE T1.COUNTY = 'RIVERSIDE' GROUP BY T1.SCHOOL HAVING AVG(T2.AVGSCRMATH) > 400;
-- ----------sql2------------
-- q, 
SELECT T1.SNAME, T2.CHARTER_FUNDING_TYPE FROM SATSCORES AS T1 INNER JOIN FRPM AS T2 ON T1.CDS = T2.CDSCODE WHERE T2.DISTRICT_NAME LIKE 'RIVERSIDE%' GROUP BY T1.SNAME, T2.CHARTER_FUNDING_TYPE HAVING CAST(SUM(T1.AVGSCRMATH) AS REAL) / COUNT(T1.CDS) > 400;
