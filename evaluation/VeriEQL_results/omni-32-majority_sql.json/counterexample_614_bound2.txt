-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (-1, 0, 'TEACHER');
INSERT INTO BADGES VALUES (0, 0, '8016088012456191006');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, -1);
INSERT INTO COMMENTS VALUES (-1, 0, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 0, 0);
INSERT INTO POSTHISTORY VALUES (1, 0, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
INSERT INTO POSTLINKS VALUES (-1, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (1, 0, -1, 0);
INSERT INTO POSTS VALUES (0, -1, -1, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 1);
INSERT INTO TAGS VALUES (-1, 0, 1);
CREATE TABLE USERS (
	ID INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (0, 0);
INSERT INTO USERS VALUES (-1, 0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 1, -1);
INSERT INTO VOTES VALUES (1, 1, -1);
-- ----------sql1------------
-- 100
SELECT (CAST(COUNT(DISTINCT CASE WHEN B.NAME = 'TEACHER' THEN B.USERID END) AS REAL) * 100.0) / COUNT(DISTINCT B.USERID) AS TEACHERBADGEPERCENTAGE FROM BADGES B;
-- ----------sql2------------
-- 50
SELECT CAST(COUNT(T1.ID) AS REAL) * 100 / (SELECT COUNT(ID) FROM USERS) FROM USERS AS T1 INNER JOIN BADGES AS T2 ON T1.ID = T2.USERID WHERE T2.NAME = 'TEACHER';
