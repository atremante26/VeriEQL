-- ----------An counterexample found by VeriEQL------------
CREATE TABLE PLAYER_ATTRIBUTES (
	ID INTEGER,
	PLAYER_FIFA_API_ID INTEGER,
	PLAYER_API_ID INTEGER,
	OVERALL_RATING INTEGER,
	STRENGTH INTEGER
);
INSERT INTO PLAYER_ATTRIBUTES VALUES (-2, 0, 1, 0, 0);
INSERT INTO PLAYER_ATTRIBUTES VALUES (-1, 0, 1, 0, 0);
INSERT INTO PLAYER_ATTRIBUTES VALUES (0, 0, 1, 0, 0);
CREATE TABLE PLAYER (
	ID INTEGER,
	PLAYER_API_ID INTEGER,
	PLAYER_FIFA_API_ID INTEGER,
	HEIGHT INTEGER
);
INSERT INTO PLAYER VALUES (-1, 1, 0, 180);
INSERT INTO PLAYER VALUES (0, 0, 0, 180);
INSERT INTO PLAYER VALUES (1, 2, 0, 180);
CREATE TABLE LEAGUE (
	ID INTEGER,
	COUNTRY_ID INTEGER
);
INSERT INTO LEAGUE VALUES (-2, 1);
INSERT INTO LEAGUE VALUES (-1, 0);
INSERT INTO LEAGUE VALUES (0, 0);
CREATE TABLE COUNTRY (
	ID INTEGER
);
INSERT INTO COUNTRY VALUES (2);
INSERT INTO COUNTRY VALUES (1);
INSERT INTO COUNTRY VALUES (0);
CREATE TABLE TEAM (
	ID INTEGER,
	TEAM_API_ID INTEGER,
	TEAM_FIFA_API_ID INTEGER
);
INSERT INTO TEAM VALUES (-1, 0, 0);
INSERT INTO TEAM VALUES (0, 0, 1);
INSERT INTO TEAM VALUES (1, 0, 1);
CREATE TABLE TEAM_ATTRIBUTES (
	ID INTEGER,
	TEAM_FIFA_API_ID INTEGER,
	TEAM_API_ID INTEGER
);
INSERT INTO TEAM_ATTRIBUTES VALUES (-1, 0, 0);
INSERT INTO TEAM_ATTRIBUTES VALUES (0, 0, 0);
INSERT INTO TEAM_ATTRIBUTES VALUES (1, 1, 0);
CREATE TABLE MATCH (
	ID INTEGER,
	COUNTRY_ID INTEGER,
	HOME_TEAM_API_ID INTEGER,
	AWAY_TEAM_API_ID INTEGER,
	HOME_PLAYER_1 INTEGER,
	HOME_PLAYER_2 INTEGER,
	HOME_PLAYER_3 INTEGER,
	HOME_PLAYER_4 INTEGER,
	HOME_PLAYER_5 INTEGER,
	HOME_PLAYER_6 INTEGER,
	HOME_PLAYER_7 INTEGER,
	HOME_PLAYER_8 INTEGER,
	HOME_PLAYER_9 INTEGER,
	HOME_PLAYER_10 INTEGER,
	HOME_PLAYER_11 INTEGER,
	AWAY_PLAYER_1 INTEGER,
	AWAY_PLAYER_2 INTEGER,
	AWAY_PLAYER_3 INTEGER,
	AWAY_PLAYER_4 INTEGER,
	AWAY_PLAYER_5 INTEGER,
	AWAY_PLAYER_6 INTEGER,
	AWAY_PLAYER_7 INTEGER,
	AWAY_PLAYER_8 INTEGER,
	AWAY_PLAYER_9 INTEGER,
	AWAY_PLAYER_10 INTEGER,
	AWAY_PLAYER_11 INTEGER
);
INSERT INTO MATCH VALUES (-2, 0, 0, 0, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 1, 2, 2, 2, 2, 0, 2, 2, 2, 2, 2, 2);
INSERT INTO MATCH VALUES (-1, 0, 0, 0, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 0, 2, 2, 2, 2, 2, 2, 2, 2);
INSERT INTO MATCH VALUES (0, 0, 0, 0, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 0, 2, 2, 2, 0, 2, 2, 2, 2, 2, 2);
-- ----------sql1------------
-- 0
SELECT CAST(SUM(CASE WHEN T1.HEIGHT < 180 AND T2.STRENGTH > 70 THEN 1 ELSE 0 END) AS REAL) * 100 / COUNT(T1.ID) AS PERCENTAGE FROM PLAYER AS T1 INNER JOIN PLAYER_ATTRIBUTES AS T2 ON T1.PLAYER_FIFA_API_ID = T2.PLAYER_FIFA_API_ID WHERE T1.HEIGHT IS NOT NULL AND T2.STRENGTH IS NOT NULL;
-- ----------sql2------------
-- NULL
SELECT CAST(COUNT(CASE WHEN T2.OVERALL_RATING > 70 THEN T1.ID ELSE NULL END) AS REAL) * 100 / COUNT(T1.ID) PERCENT FROM PLAYER AS T1 INNER JOIN PLAYER_ATTRIBUTES AS T2 ON T1.PLAYER_API_ID = T2.PLAYER_API_ID WHERE T1.HEIGHT < 180;
