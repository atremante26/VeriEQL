-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER
);
INSERT INTO ALIGNMENT VALUES (-1);
INSERT INTO ALIGNMENT VALUES (0);
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (0, 'SPEED');
INSERT INTO ATTRIBUTE VALUES (-1, 'SPEED');
CREATE TABLE COLOUR (
	ID INTEGER
);
INSERT INTO COLOUR VALUES (1);
INSERT INTO COLOUR VALUES (0);
CREATE TABLE GENDER (
	ID INTEGER
);
INSERT INTO GENDER VALUES (0);
INSERT INTO GENDER VALUES (-1);
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (0, '2147483648');
INSERT INTO PUBLISHER VALUES (-1, '2147483649');
CREATE TABLE RACE (
	ID INTEGER
);
INSERT INTO RACE VALUES (-1);
INSERT INTO RACE VALUES (0);
CREATE TABLE SUPERHERO (
	ID INTEGER,
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER
);
INSERT INTO SUPERHERO VALUES (1, -1, 0, 0, 0, 0, 0, 0);
INSERT INTO SUPERHERO VALUES (0, 0, 1, 0, 1, -1, -1, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (1, -1, NULL);
INSERT INTO HERO_ATTRIBUTE VALUES (0, 0, NULL);
CREATE TABLE SUPERPOWER (
	ID INTEGER
);
INSERT INTO SUPERPOWER VALUES (-1);
INSERT INTO SUPERPOWER VALUES (0);
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (1, 0);
INSERT INTO HERO_POWER VALUES (1, -1);
-- ----------sql1------------
-- 2147483648
SELECT T4.PUBLISHER_NAME FROM ATTRIBUTE AS T1 INNER JOIN HERO_ATTRIBUTE AS T2 ON T1.ID = T2.ATTRIBUTE_ID INNER JOIN SUPERHERO AS T3 ON T2.HERO_ID = T3.ID INNER JOIN PUBLISHER AS T4 ON T3.PUBLISHER_ID = T4.ID WHERE T1.ATTRIBUTE_NAME = 'SPEED' ORDER BY T2.ATTRIBUTE_VALUE LIMIT 1;
-- ----------sql2------------
-- 2147483649
SELECT T2.PUBLISHER_NAME FROM SUPERHERO AS T1 INNER JOIN PUBLISHER AS T2 ON T1.PUBLISHER_ID = T2.ID INNER JOIN HERO_ATTRIBUTE AS T3 ON T1.ID = T3.HERO_ID INNER JOIN ATTRIBUTE AS T4 ON T3.ATTRIBUTE_ID = T4.ID WHERE T4.ATTRIBUTE_NAME = 'SPEED' ORDER BY T3.ATTRIBUTE_VALUE LIMIT 1;
