-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-1, 0);
INSERT INTO BADGES VALUES (0, 0);
INSERT INTO BADGES VALUES (1, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, 2, 0);
INSERT INTO COMMENTS VALUES (0, 2, 0);
INSERT INTO COMMENTS VALUES (1, 2, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, 1, 0);
INSERT INTO POSTHISTORY VALUES (0, 2, 0);
INSERT INTO POSTHISTORY VALUES (1, 2, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-2, 2, 1);
INSERT INTO POSTLINKS VALUES (-1, 2, 2);
INSERT INTO POSTLINKS VALUES (0, 2, 2);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (1, 0, 0, 2);
INSERT INTO POSTS VALUES (0, 0, 0, 2);
INSERT INTO POSTS VALUES (2, 0, 0, 2);
CREATE TABLE TAGS (
	ID INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (1, 2);
INSERT INTO TAGS VALUES (2, 2);
INSERT INTO TAGS VALUES (0, 2);
CREATE TABLE USERS (
	ID INTEGER,
	REPUTATION INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (2, 2, 0);
INSERT INTO USERS VALUES (1, 3, 1);
INSERT INTO USERS VALUES (0, 3, 0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 2, 0);
INSERT INTO VOTES VALUES (-1, 2, 0);
INSERT INTO VOTES VALUES (0, 2, 0);
-- ----------sql1------------
-- 1
SELECT AGE FROM USERS WHERE REPUTATION IS NOT NULL ORDER BY REPUTATION DESC LIMIT 1;
-- ----------sql2------------
-- 1
-- 0
SELECT AGE FROM USERS WHERE REPUTATION = ( SELECT MAX(REPUTATION) FROM USERS );
