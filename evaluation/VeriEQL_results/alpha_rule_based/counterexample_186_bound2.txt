-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (2, 1, 'POPLATEK TYDNE', '0001-01-01');
INSERT INTO ACCOUNT VALUES (3, 2, 'POPLATEK TYDNE', '0001-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20),
	ISSUED DATE
);
INSERT INTO CARD VALUES (1, 10, 'classic', '0001-01-01');
INSERT INTO CARD VALUES (2, 9, 'junior', '0001-01-02');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	GENDER VARCHAR(20),
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (2, 'F', '0001-01-01', 2);
INSERT INTO CLIENT VALUES (1, 'M', '0001-01-01', 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (9, 2, 2, 'DISPONENT');
INSERT INTO DISP VALUES (10, 1, 2, 'DISPONENT');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A2 VARCHAR(20),
	A3 VARCHAR(20),
	A4 VARCHAR(20),
	A5 VARCHAR(20),
	A6 VARCHAR(20),
	A7 VARCHAR(20),
	A8 INTEGER,
	A9 INTEGER,
	A10 INTEGER,
	A11 INTEGER,
	A12 INTEGER,
	A13 INTEGER,
	A14 INTEGER,
	A15 INTEGER,
	A16 INTEGER
);
INSERT INTO DISTRICT VALUES (1, '2147483648', 'south Moravia', '2147483648', '2147483648', '2147483648', '8', 4, 11, 34, 8110, 1, 1, 81, 818, 888);
INSERT INTO DISTRICT VALUES (2, '2147483649', 'east Bohemia', '2147483649', '2147483649', '2147483649', '4', 4, 2, 35, 8111, 2, 2, 82, 819, 889);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER,
	PAYMENTS INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (4959, 3, '0001-01-01', 4980, 24, 304, 'D');
INSERT INTO LOAN VALUES (4960, 2, '0001-01-02', 4981, 24, 305, 'A');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	BANK_TO VARCHAR(20),
	ACCOUNT_TO INTEGER,
	AMOUNT INTEGER,
	K_SYMBOL VARCHAR(20)
);
INSERT INTO ORDER VALUES (29401, 2, 'IJ', 399, 1, 'POJISTNE');
INSERT INTO ORDER VALUES (29402, 2, 'WX', 399, 1, 'LEASING');
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	TYPE VARCHAR(20),
	OPERATION VARCHAR(20),
	AMOUNT INTEGER,
	BALANCE INTEGER,
	K_SYMBOL VARCHAR(20),
	BANK VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (1, 2, '0001-01-01', 'VYDAJ', 'PREVOD Z UCTU', 0, -41126, 'UVER', 'QR', 0);
INSERT INTO TRANS VALUES (2, 2, '0001-01-01', 'VYBER', 'VYBER', 0, -41126, 'DUCHOD', 'YZ', 0);
-- ----------sql1------------
-- 50
SELECT CAST(COUNT(CASE WHEN CLIENT.GENDER = 'M' THEN 1 ELSE NULL END) AS REAL) * 100 / COUNT(*) AS PERCENTAGE FROM ACCOUNT INNER JOIN DISP ON ACCOUNT.ACCOUNT_ID = DISP.ACCOUNT_ID INNER JOIN CLIENT ON DISP.CLIENT_ID = CLIENT.CLIENT_ID WHERE ACCOUNT.FREQUENCY = 'POPLATEK TYDNE';
-- ----------sql2------------
-- 100
SELECT CAST(SUM(T1.GENDER = 'M') AS REAL) * 100 / COUNT(T1.CLIENT_ID) FROM CLIENT AS T1 INNER JOIN DISTRICT AS T3 ON T1.DISTRICT_ID = T3.DISTRICT_ID INNER JOIN ACCOUNT AS T2 ON T2.DISTRICT_ID = T3.DISTRICT_ID INNER JOIN DISP AS T4 ON T1.CLIENT_ID = T4.CLIENT_ID AND T2.ACCOUNT_ID = T4.ACCOUNT_ID WHERE T2.FREQUENCY = 'POPLATEK TYDNE';
