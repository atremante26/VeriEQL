-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (-1, '4000-02-02', NULL);
INSERT INTO EXAMINATION VALUES (-1, '4001-12-02', '');
INSERT INTO EXAMINATION VALUES (0, '4002-03-01', '');
CREATE TABLE PATIENT (
	ID INTEGER,
	BIRTHDAY DATE,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (-2, '4000-12-01', NULL);
INSERT INTO PATIENT VALUES (0, NULL, NULL);
INSERT INTO PATIENT VALUES (-1, '4100-11-01', '');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	HGB INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '2009-10-01', -1, '', '-BB');
INSERT INTO LABORATORY VALUES (0, '2008-03-30', -2, '', NULL);
INSERT INTO LABORATORY VALUES (-2, '2008-03-31', 0, '', NULL);
-- ----------sql1------------
-- -100, NULL
SELECT (STRFTIME('%Y', T1.EXAMINATION_DATE) - STRFTIME('%Y', T3.BIRTHDAY)) AS AGE, T1.DIAGNOSIS FROM EXAMINATION AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID INNER JOIN PATIENT AS T3 ON T2.ID = T3.ID ORDER BY T2.HGB DESC LIMIT 1;
-- ----------sql2------------
-- -1992, NULL
SELECT STRFTIME('%Y', T2.DATE) - STRFTIME('%Y', T1.BIRTHDAY), T1.DIAGNOSIS FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID ORDER BY T2.HGB DESC LIMIT 1;
