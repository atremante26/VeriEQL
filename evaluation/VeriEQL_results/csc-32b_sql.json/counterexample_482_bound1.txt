-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CARDS (
	ID INTEGER,
	NAME VARCHAR(20),
	SETCODE VARCHAR(20),
	TYPE VARCHAR(20),
	UUID VARCHAR(20)
);
INSERT INTO CARDS VALUES (0, 'ANCESTORS CHOSEN', '', '', '');
CREATE TABLE FOREIGN_DATA (
	ID INTEGER,
	LANGUAGE VARCHAR(20),
	NAME VARCHAR(20),
	TYPE VARCHAR(20),
	UUID VARCHAR(20)
);
INSERT INTO FOREIGN_DATA VALUES (0, 'GERMAN', '', '', '');
CREATE TABLE LEGALITIES (
	ID INTEGER,
	UUID VARCHAR(20)
);
INSERT INTO LEGALITIES VALUES (0, '');
CREATE TABLE SETS (
	ID INTEGER,
	CODE VARCHAR(20),
	NAME VARCHAR(20),
	TYPE VARCHAR(20)
);
INSERT INTO SETS VALUES (0, '', '', '');
CREATE TABLE SET_TRANSLATIONS (
	ID INTEGER,
	LANGUAGE VARCHAR(20),
	SETCODE VARCHAR(20)
);
INSERT INTO SET_TRANSLATIONS VALUES (0, '', '');
CREATE TABLE RULINGS (
	ID INTEGER,
	UUID VARCHAR(20)
);
INSERT INTO RULINGS VALUES (0, '');
-- ----------sql1------------
-- 
SELECT T2.TYPE FROM CARDS AS T1 INNER JOIN FOREIGN_DATA AS T2 ON T1.UUID = T2.UUID WHERE T1.NAME LIKE 'ANCESTOR%S CHOSEN' AND T2.LANGUAGE = 'GERMAN';
-- ----------sql2------------
SELECT DISTINCT T1.TYPE FROM CARDS AS T1 INNER JOIN FOREIGN_DATA AS T2 ON T2.UUID = T1.UUID WHERE T1.NAME = 'ANCESTOR''S CHOSEN' AND T2.LANGUAGE = 'GERMAN';
