-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (0, 0, '2147483648');
INSERT INTO BADGES VALUES (-1, -1, '2147483648');
INSERT INTO BADGES VALUES (-2, -1, '2147483648');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, -1, -1);
INSERT INTO COMMENTS VALUES (-2, 1, 1);
INSERT INTO COMMENTS VALUES (0, -1, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 1, 1);
INSERT INTO POSTHISTORY VALUES (-1, 0, 1);
INSERT INTO POSTHISTORY VALUES (-2, 0, 1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, 1, -1);
INSERT INTO POSTLINKS VALUES (0, 1, -1);
INSERT INTO POSTLINKS VALUES (-2, 1, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	VIEWCOUNT INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, -1, -1, -1);
INSERT INTO POSTS VALUES (-1, 1, -1, 1, -1);
INSERT INTO POSTS VALUES (1, -1, 0, 0, -1);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (2, 0, -1);
INSERT INTO TAGS VALUES (0, 0, 1);
INSERT INTO TAGS VALUES (1, 0, 1);
CREATE TABLE USERS (
	ID INTEGER,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, '8793021453127183719');
INSERT INTO USERS VALUES (1, 'NOAH SNYDER');
INSERT INTO USERS VALUES (0, 'HARVEY MOTULSKY');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 1, 0);
INSERT INTO VOTES VALUES (0, -1, -1);
INSERT INTO VOTES VALUES (-1, -1, 0);
-- ----------sql1------------
-- HARVEY MOTULSKY
SELECT DISPLAYNAME FROM ( SELECT U.DISPLAYNAME, SUM(P.VIEWCOUNT) AS TOTALVIEWCOUNT FROM USERS U JOIN POSTS P ON U.ID = P.OWNERUSERID WHERE U.DISPLAYNAME IN ('HARVEY MOTULSKY', 'NOAH SNYDER') GROUP BY U.DISPLAYNAME ORDER BY TOTALVIEWCOUNT DESC LIMIT 1 ) AS HIGHERPOPULARITYUSER;
-- ----------sql2------------
-- NOAH SNYDER
SELECT T1.DISPLAYNAME FROM USERS AS T1 INNER JOIN POSTHISTORY AS T2 ON T1.ID = T2.USERID INNER JOIN POSTS AS T3 ON T2.POSTID = T3.ID WHERE T1.DISPLAYNAME = 'HARVEY MOTULSKY' OR T1.DISPLAYNAME = 'NOAH SNYDER' GROUP BY T1.DISPLAYNAME ORDER BY SUM(T3.VIEWCOUNT) DESC LIMIT 1;
