-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CARDS (
	ID INTEGER,
	ARTIST VARCHAR(20),
	AVAILABILITY VARCHAR(20),
	SETCODE VARCHAR(20),
	UUID VARCHAR(20)
);
INSERT INTO CARDS VALUES (0, 'ERICA YANG', 'PAPER', '2147483648', '2147483648');
CREATE TABLE FOREIGN_DATA (
	ID INTEGER,
	UUID VARCHAR(20)
);
INSERT INTO FOREIGN_DATA VALUES (0, '2147483648');
CREATE TABLE LEGALITIES (
	ID INTEGER,
	FORMAT VARCHAR(20),
	STATUS VARCHAR(20),
	UUID VARCHAR(20)
);
INSERT INTO LEGALITIES VALUES (0, 'PAUPER', '8857954166960298848', '2147483648');
CREATE TABLE SETS (
	ID INTEGER,
	CODE VARCHAR(20)
);
INSERT INTO SETS VALUES (0, '2147483648');
CREATE TABLE SET_TRANSLATIONS (
	ID INTEGER,
	SETCODE VARCHAR(20)
);
INSERT INTO SET_TRANSLATIONS VALUES (0, '2147483648');
CREATE TABLE RULINGS (
	ID INTEGER,
	UUID VARCHAR(20)
);
INSERT INTO RULINGS VALUES (0, '2147483648');
-- ----------sql1------------
SELECT C.ID FROM CARDS C INNER JOIN LEGALITIES L ON C.UUID = L.UUID WHERE C.ARTIST = 'ERICA YANG' AND L.FORMAT = 'PAUPER' AND L.STATUS = 'LEGAL' AND C.AVAILABILITY = 'PAPER';
-- ----------sql2------------
-- 0
SELECT T1.ID FROM CARDS AS T1 INNER JOIN LEGALITIES AS T2 ON T1.UUID = T2.UUID WHERE T1.ARTIST = 'ERICA YANG' AND T2.FORMAT = 'PAUPER' AND T1.AVAILABILITY = 'PAPER';
