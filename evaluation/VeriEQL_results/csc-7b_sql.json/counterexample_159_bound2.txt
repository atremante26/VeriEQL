-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (-1, -1);
INSERT INTO ACCOUNT VALUES (0, -1);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (-1, 0);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, -1);
INSERT INTO CLIENT VALUES (3356, -1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 3356, 0);
INSERT INTO DISP VALUES (1, 3356, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (-1);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (-1, 0);
INSERT INTO LOAN VALUES (0, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
INSERT INTO ORDER VALUES (1, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	OPERATION VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (-1, 0, 'VYBER', 0);
INSERT INTO TRANS VALUES (0, 0, 'VYBER', 0);
-- ----------sql1------------
-- VYBER
-- VYBER
SELECT OPERATION FROM TRANS WHERE ACCOUNT_ID IN (SELECT ACCOUNT_ID FROM DISP WHERE CLIENT_ID = 3356) AND OPERATION = 'VYBER';
-- ----------sql2------------
-- -1
-- -1
-- 0
-- 0
SELECT T4.TRANS_ID FROM CLIENT AS T1 INNER JOIN DISP AS T2 ON T1.CLIENT_ID = T2.CLIENT_ID INNER JOIN ACCOUNT AS T3 ON T2.ACCOUNT_ID = T3.ACCOUNT_ID INNER JOIN TRANS AS T4 ON T3.ACCOUNT_ID = T4.ACCOUNT_ID WHERE T1.CLIENT_ID = 3356 AND T4.OPERATION = 'VYBER';
