-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, 0, '2020-10-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A4 VARCHAR(20),
	A15 INTEGER
);
INSERT INTO DISTRICT VALUES (0, '0', 4001);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE
);
INSERT INTO LOAN VALUES (0, 0, '1000-05-01');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, '1000-09-01', 0);
-- ----------sql1------------
-- NULL
SELECT AVG(T1.A15) FROM DISTRICT AS T1 INNER JOIN ACCOUNT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE T1.A4 > 4000 AND T2.DATE >= '1997-01-01' AND T2.DATE < '1998-01-01';
-- ----------sql2------------
-- 4001
SELECT AVG(T1.A15) FROM DISTRICT AS T1 INNER JOIN ACCOUNT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE STRFTIME('%Y', T2.DATE) >= '1997' AND T1.A15 > 4000;
