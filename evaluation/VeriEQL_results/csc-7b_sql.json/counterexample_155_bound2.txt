-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (1, 0);
INSERT INTO ACCOUNT VALUES (0, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20),
	ISSUED DATE
);
INSERT INTO CARD VALUES (0, 0, 'GOLD', NULL);
INSERT INTO CARD VALUES (1, 0, NULL, '1042-12-01');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
INSERT INTO CLIENT VALUES (1, 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (1, 1, 0, '');
INSERT INTO DISP VALUES (0, 1, 1, '');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (1);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (1, 0);
INSERT INTO LOAN VALUES (0, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (1, 1);
INSERT INTO ORDER VALUES (0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO TRANS VALUES (1, 1, '');
INSERT INTO TRANS VALUES (0, 0, '');
-- ----------sql1------------
-- 0
SELECT CAST(COUNT(CASE WHEN TYPE = 'GOLD' AND STRFTIME('%Y', ISSUED) < '1998' THEN CARD_ID END) AS REAL) * 100 / COUNT(CARD_ID) FROM CARD;
-- ----------sql2------------
-- NULL
SELECT CAST(SUM(TYPE = 'GOLD' AND STRFTIME('%Y', ISSUED) < '1998') AS REAL) * 100 / COUNT(CARD_ID) FROM CARD;
