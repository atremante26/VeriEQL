-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20),
	DISTRICT_NAME VARCHAR(20),
	CHARTER_FUNDING_TYPE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('Ä', '', '[');
INSERT INTO FRPM VALUES ('', 'RIVERSIDE\u{121}', 'W');
INSERT INTO FRPM VALUES ('\u{126}æ', '©', '');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20),
	SNAME VARCHAR(20),
	AVGSCRMATH INTEGER
);
INSERT INTO SATSCORES VALUES ('', NULL, 401);
INSERT INTO SATSCORES VALUES ('\u{126}æ', '', 401);
INSERT INTO SATSCORES VALUES ('Ä', '', -10);
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	COUNTY VARCHAR(20),
	DISTRICT VARCHAR(20),
	SCHOOL VARCHAR(20),
	CHARTER INTEGER,
	FUNDINGTYPE VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('\u{126}æ', 'c', '', '', 0, '');
INSERT INTO SCHOOLS VALUES ('Ä', 'l', '', '', 0, '');
INSERT INTO SCHOOLS VALUES ('', 'v', '', '', 0, '');
-- ----------sql1------------
SELECT T2.SCHOOL, T2.FUNDINGTYPE FROM SATSCORES AS T1 JOIN SCHOOLS AS T2 ON T1.CDS = T2.CDSCODE WHERE T2.COUNTY = 'RIVERSIDE' AND T1.AVGSCRMATH > 400;
-- ----------sql2------------
-- NULL, W
SELECT T1.SNAME, T2.CHARTER_FUNDING_TYPE FROM SATSCORES AS T1 INNER JOIN FRPM AS T2 ON T1.CDS = T2.CDSCODE WHERE T2.DISTRICT_NAME LIKE 'RIVERSIDE%' GROUP BY T1.SNAME, T2.CHARTER_FUNDING_TYPE HAVING CAST(SUM(T1.AVGSCRMATH) AS REAL) / COUNT(T1.CDS) > 400;
