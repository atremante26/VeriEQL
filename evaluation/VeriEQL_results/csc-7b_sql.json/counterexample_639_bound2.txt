-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (0, 1, '2147483648');
INSERT INTO BADGES VALUES (1, 0, '2147483648');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, 1);
INSERT INTO COMMENTS VALUES (1, 0, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, 0, 0);
INSERT INTO POSTHISTORY VALUES (0, 1, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, 0, 0);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	TAGS VARCHAR(20),
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 1, '2147483648', 0, 1);
INSERT INTO POSTS VALUES (1, 1, '2147483648', 1, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	TAGNAME VARCHAR(20),
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, '5784509981357962619', NULL, 0);
INSERT INTO TAGS VALUES (1, '5784509981357962621', 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	DISPLAYNAME VARCHAR(20),
	AGE INTEGER
);
INSERT INTO USERS VALUES (1, 'COMMUNITY', 0);
INSERT INTO USERS VALUES (0, '1560088996988633875', 0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-1, 1, 1);
INSERT INTO VOTES VALUES (0, 1, 1);
-- ----------sql1------------
-- 0
SELECT CAST(SUM(CASE WHEN T2.TAGNAME = 'R' THEN 1 ELSE 0 END) AS REAL) * 100 / COUNT(*) AS PERCENTAGE FROM POSTS AS T1 INNER JOIN USERS AS T3 ON T1.OWNERUSERID = T3.ID INNER JOIN POSTS AS T4 ON T4.ID = T1.ID INNER JOIN TAGS AS T2 ON T2.EXCERPTPOSTID = T4.ID WHERE T3.DISPLAYNAME = 'COMMUNITY';
-- ----------sql2------------
-- NULL
SELECT CAST(SUM(IIF(T3.TAGNAME = 'R', 1, 0)) AS REAL) * 100 / COUNT(T1.ID) FROM USERS AS T1 INNER JOIN POSTHISTORY AS T2 ON T1.ID = T2.USERID INNER JOIN TAGS AS T3 ON T3.EXCERPTPOSTID = T2.POSTID WHERE T1.DISPLAYNAME = 'COMMUNITY';
