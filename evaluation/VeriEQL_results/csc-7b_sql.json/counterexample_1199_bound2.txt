-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (-1);
CREATE TABLE PATIENT (
	ID INTEGER
);
INSERT INTO PATIENT VALUES (0);
INSERT INTO PATIENT VALUES (-1);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	TP INTEGER,
	ALB INTEGER,
	UN INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1997-10-01', 6, 0, 0, '', '');
INSERT INTO LABORATORY VALUES (-1, '1997-01-01', 6, 0, 0, '', '');
-- ----------sql1------------
-- 0
SELECT COUNT(T2.ID) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE STRFTIME('%Y', T2.DATE) = '1997' AND (T2.TP < 6 OR T2.TP > 8.5);
-- ----------sql2------------
-- 2
SELECT COUNT(ID) FROM LABORATORY WHERE (ALB <= 6.0 OR ALB >= 8.5) AND STRFTIME('%Y', DATE) = '1997';
