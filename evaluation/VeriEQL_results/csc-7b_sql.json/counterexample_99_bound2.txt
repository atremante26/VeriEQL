-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (-1, 1, '1993-10-01');
INSERT INTO ACCOUNT VALUES (0, 0, '1993-05-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (1, 0);
INSERT INTO CARD VALUES (0, 1);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (-1, 1);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, -1, 0);
INSERT INTO DISP VALUES (1, -1, -1);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
INSERT INTO DISTRICT VALUES (1);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER
);
INSERT INTO LOAN VALUES (1, 0, '1000-10-01', -1, 13);
INSERT INTO LOAN VALUES (0, -1, '1000-11-01', -1, 13);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	AMOUNT INTEGER
);
INSERT INTO ORDER VALUES (-1, -1, 0);
INSERT INTO ORDER VALUES (0, -1, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, -1, '1000-04-01', 0, 0);
INSERT INTO TRANS VALUES (-1, 0, '1000-01-01', 0, 0);
-- ----------sql1------------
-- 0
SELECT T1.ACCOUNT_ID FROM ACCOUNT AS T1 JOIN LOAN AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T2.DURATION > 12 AND STRFTIME('%Y', T1.DATE) = '1993' ORDER BY T2.AMOUNT DESC LIMIT 1;
-- ----------sql2------------
-- -1
SELECT T1.ACCOUNT_ID FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE STRFTIME('%Y', T2.DATE) = '1993' AND T1.DURATION > 12 ORDER BY T1.AMOUNT DESC LIMIT 1;
