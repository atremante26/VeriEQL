-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (0, 2);
INSERT INTO BADGES VALUES (1, 0);
INSERT INTO BADGES VALUES (2, 2);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	SCORE INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, -1, 0, 2);
INSERT INTO COMMENTS VALUES (1, 0, 0, 2);
INSERT INTO COMMENTS VALUES (0, 1, 0, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER,
	COMMENT VARCHAR(20)
);
INSERT INTO POSTHISTORY VALUES (-2, 1, 0, '2147483648');
INSERT INTO POSTHISTORY VALUES (0, -1, 0, '2147483648');
INSERT INTO POSTHISTORY VALUES (-1, 0, 2, '2147483648');
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, 0, 0);
INSERT INTO POSTLINKS VALUES (1, 1, 0);
INSERT INTO POSTLINKS VALUES (0, 1, 1);
CREATE TABLE POSTS (
	ID INTEGER,
	SCORE INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-1, 0, 2, 2, 1);
INSERT INTO POSTS VALUES (1, 0, 2, 2, 0);
INSERT INTO POSTS VALUES (0, 0, 2, 2, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-2, 0, -1);
INSERT INTO TAGS VALUES (-1, 0, 0);
INSERT INTO TAGS VALUES (0, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (1, 0);
INSERT INTO USERS VALUES (0, 40);
INSERT INTO USERS VALUES (2, 40);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 0, 2);
INSERT INTO VOTES VALUES (1, 0, 2);
INSERT INTO VOTES VALUES (2, 0, 2);
-- ----------sql1------------
-- 2
SELECT COUNT(DISTINCT T2.ID) FROM COMMENTS AS T1 INNER JOIN USERS AS T2 ON T1.USERID = T2.ID WHERE T1.SCORE = 0 AND T2.AGE = 40;
-- ----------sql2------------
-- 3
SELECT COUNT(DISTINCT T1.ID) FROM COMMENTS AS T1 INNER JOIN USERS AS T2 ON T1.USERID = T2.ID WHERE T1.SCORE = 0 AND T2.AGE = 40;
