-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('2147483648');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20),
	NUMTSTTAKR INTEGER
);
INSERT INTO SATSCORES VALUES ('2147483648', 0);
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('2147483648', 'MERGED', '6494692515793773862', '2147483648');
-- ----------sql1------------
-- 1
SELECT COUNT(CASE WHEN T1.STATUSTYPE = 'MERGED' AND T2.NUMTSTTAKR < 100 THEN T1.CDSCODE END) FROM SCHOOLS AS T1 INNER JOIN SATSCORES AS T2 ON T1.CDSCODE = T2.CDS;
-- ----------sql2------------
-- 0
SELECT COUNT(T1.CDSCODE) FROM SCHOOLS AS T1 INNER JOIN SATSCORES AS T2 ON T1.CDSCODE = T2.CDS WHERE T1.STATUSTYPE = 'MERGED' AND T2.NUMTSTTAKR < 100 AND T1.COUNTY = 'LAKE';
