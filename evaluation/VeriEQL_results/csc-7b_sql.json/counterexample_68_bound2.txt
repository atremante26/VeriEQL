-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('');
INSERT INTO FRPM VALUES ('');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('');
INSERT INTO SATSCORES VALUES ('');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20),
	CLOSEDDATE DATE,
	SOC VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('', '', '', '', '1000-11-01', '0');
INSERT INTO SCHOOLS VALUES ('', '', NULL, '', '1980-02-01', '11');
-- ----------sql1------------
-- NULL
SELECT T1.COUNTY FROM SCHOOLS AS T1 WHERE T1.CLOSEDDATE BETWEEN '1980-01-01' AND '1989-12-31' AND T1.SOC = 11 GROUP BY T1.COUNTY ORDER BY COUNT(*) DESC LIMIT 1;
-- ----------sql2------------
SELECT COUNTY FROM SCHOOLS WHERE STRFTIME('%Y', CLOSEDDATE) BETWEEN '1980' AND '1989' AND STATUSTYPE = 'CLOSED' AND SOC = 11 GROUP BY COUNTY ORDER BY COUNT(SCHOOL) DESC LIMIT 1;
