-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (130, 0, '1000-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 131);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, '2280-12-01', 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (131, 0, 130);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE
);
INSERT INTO LOAN VALUES (0, 130, '1000-12-01');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 130);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 130, '1000-02-01', 0);
-- ----------sql1------------
SELECT STRFTIME('%Y', T1.BIRTH_DATE) FROM CLIENT AS T1 JOIN DISP AS T2 ON T1.CLIENT_ID = T2.CLIENT_ID WHERE T2.DISP_ID = 130;
-- ----------sql2------------
-- 2280
SELECT STRFTIME('%Y', T1.BIRTH_DATE) FROM CLIENT AS T1 INNER JOIN DISP AS T3 ON T1.CLIENT_ID = T3.CLIENT_ID INNER JOIN ACCOUNT AS T2 ON T3.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T2.ACCOUNT_ID = 130;
