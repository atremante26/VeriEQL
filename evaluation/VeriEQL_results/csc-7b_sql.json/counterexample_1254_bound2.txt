-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (-1);
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (-1, '4000-10-01');
INSERT INTO PATIENT VALUES (0, '2000-02-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	IGA INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '1000-05-01', 0, 80, '', '');
INSERT INTO LABORATORY VALUES (0, '1000-12-01', 0, 499, '', '');
-- ----------sql1------------
-- 1
SELECT COUNT(T2.ID) FROM LABORATORY AS T1 INNER JOIN PATIENT AS T2 ON T1.ID = T2.ID WHERE T1.IGA > 80 AND T1.IGA < 500 AND STRFTIME('%Y', T2.FIRST_DATE) >= '1990';
-- ----------sql2------------
-- 2
SELECT COUNT(T1.ID) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.IGA BETWEEN 80 AND 500 AND  STRFTIME('%Y',  T1.FIRST_DATE) > '1990';
