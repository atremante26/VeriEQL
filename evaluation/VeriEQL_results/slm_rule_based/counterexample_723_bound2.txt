-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (2, 'N/A');
INSERT INTO ALIGNMENT VALUES (1, 'Bad');
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (2, 'Speed');
INSERT INTO ATTRIBUTE VALUES (1, 'Durability');
CREATE TABLE COLOUR (
	ID INTEGER,
	COLOUR VARCHAR(20)
);
INSERT INTO COLOUR VALUES (12, 'BLUE');
INSERT INTO COLOUR VALUES (33, 'BLUE');
CREATE TABLE GENDER (
	ID INTEGER,
	GENDER VARCHAR(20)
);
INSERT INTO GENDER VALUES (1, 'Male');
INSERT INTO GENDER VALUES (2, 'Female');
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (6, 'Star Trek');
INSERT INTO PUBLISHER VALUES (23, 'J. K. Rowling');
CREATE TABLE RACE (
	ID INTEGER,
	RACE VARCHAR(20)
);
INSERT INTO RACE VALUES (1, '2147483648');
INSERT INTO RACE VALUES (2, '2147483648');
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	FULL_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER,
	HEIGHT_CM INTEGER,
	WEIGHT_KG INTEGER
);
INSERT INTO SUPERHERO VALUES (1, '2147483649', '2147483648', 2, 12, 12, 33, 1, 6, 1, 0, 0);
INSERT INTO SUPERHERO VALUES (2, '2147483648', '2147483648', 2, 12, 12, 33, 2, 6, 2, 0, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 1, 10);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 1, 40);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (2, 'AGILITY');
INSERT INTO SUPERPOWER VALUES (1, 'AGILITY');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (2, 1);
INSERT INTO HERO_POWER VALUES (2, 1);
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT SUPERHERO.ID) AS COUNT FROM HERO_POWER INNER JOIN SUPERHERO ON HERO_POWER.HERO_ID = SUPERHERO.ID INNER JOIN SUPERPOWER ON HERO_POWER.POWER_ID = SUPERPOWER.ID INNER JOIN COLOUR ON SUPERHERO.EYE_COLOUR_ID = COLOUR.ID WHERE SUPERPOWER.POWER_NAME = 'AGILITY' AND COLOUR.COLOUR = 'BLUE';
-- ----------sql2------------
-- 2
SELECT COUNT(T1.ID) FROM SUPERHERO AS T1 INNER JOIN HERO_POWER AS T2 ON T1.ID = T2.HERO_ID INNER JOIN SUPERPOWER AS T3 ON T2.POWER_ID = T3.ID INNER JOIN COLOUR AS T4 ON T1.EYE_COLOUR_ID = T4.ID WHERE T3.POWER_NAME = 'AGILITY' AND T4.COLOUR = 'BLUE';
