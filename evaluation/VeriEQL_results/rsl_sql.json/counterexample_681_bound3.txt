-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20),
	DATE DATE
);
INSERT INTO BADGES VALUES (0, 0, '', '1000-12-01');
INSERT INTO BADGES VALUES (-1, 0, '', '1000-09-01');
INSERT INTO BADGES VALUES (1, 0, '', '1000-12-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, 0, 1);
INSERT INTO COMMENTS VALUES (1, 0, 1);
INSERT INTO COMMENTS VALUES (0, 0, 1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, -1, 0);
INSERT INTO POSTHISTORY VALUES (1, 0, 2);
INSERT INTO POSTHISTORY VALUES (2, -1, 1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-2, 0, 0);
INSERT INTO POSTLINKS VALUES (-1, -1, 0);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	CREAIONDATE DATE,
	VIEWCOUNT INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-2, '2012-12-01', 20001, 0, 0, -2);
INSERT INTO POSTS VALUES (-1, '2010-01-01', 20001, 2, 0, 0);
INSERT INTO POSTS VALUES (0, '2011-01-01', 20001, 1, 0, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, -2);
INSERT INTO TAGS VALUES (0, 0, 0);
INSERT INTO TAGS VALUES (1, 0, -2);
CREATE TABLE USERS (
	ID INTEGER,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (2, 'G');
INSERT INTO USERS VALUES (1, 'G');
INSERT INTO USERS VALUES (0, 'E');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 0, 0);
INSERT INTO VOTES VALUES (-1, 0, 0);
INSERT INTO VOTES VALUES (0, 0, 0);
-- ----------sql1------------
-- E
-- G
SELECT DISTINCT USERS.DISPLAYNAME FROM USERS INNER JOIN POSTS ON USERS.ID = POSTS.OWNERUSERID WHERE POSTS.VIEWCOUNT > 20000;
-- ----------sql2------------
-- G
SELECT T1.DISPLAYNAME FROM USERS AS T1 INNER JOIN POSTS AS T2 ON T1.ID = T2.OWNERUSERID WHERE STRFTIME('%Y', T2.CREAIONDATE) = '2011' AND T2.VIEWCOUNT > 20000;
