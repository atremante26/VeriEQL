-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE
);
INSERT INTO EXAMINATION VALUES (-2147483648, '1000-01-02');
CREATE TABLE PATIENT (
	ID INTEGER
);
INSERT INTO PATIENT VALUES (-2147483648);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	ALB INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-2147483648, '1000-01-01', 4, '2147483648');
-- ----------sql1------------
-- 2
SELECT EXAMINATION.EXAMINATION_DATE FROM EXAMINATION INNER JOIN LABORATORY ON EXAMINATION.ID = LABORATORY.ID WHERE LABORATORY.ALB = (SELECT MAX(ALB) FROM LABORATORY WHERE ALB > 3.5 AND ALB < 5.5 AND LABORATORY.ID = EXAMINATION.ID);
-- ----------sql2------------
-- 1
SELECT DATE FROM LABORATORY WHERE ALB > 3.5 AND ALB < 5.5 ORDER BY ALB DESC LIMIT 1;
