-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (1, 1464);
INSERT INTO BADGES VALUES (0, 1465);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, 1464);
INSERT INTO COMMENTS VALUES (1, 0, 1464);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (1, 1, 1464);
INSERT INTO POSTHISTORY VALUES (0, 1, 1464);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
INSERT INTO POSTLINKS VALUES (1, 1, 1);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	FAVORITECOUNT INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (1, 1465, 2, 1464, 0);
INSERT INTO POSTS VALUES (0, 1464, 2, 1464, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 1);
INSERT INTO TAGS VALUES (1, 0, 1);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (1464);
INSERT INTO USERS VALUES (1465);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 0, 1465);
INSERT INTO VOTES VALUES (1, 1, 1465);
-- ----------sql1------------
-- 0
SELECT POSTS.ID FROM POSTS JOIN VOTES ON POSTS.ID = VOTES.POSTID WHERE VOTES.USERID = 1465 ORDER BY POSTS.FAVORITECOUNT DESC LIMIT 1;
-- ----------sql2------------
-- 1
SELECT T2.ID FROM VOTES AS T1 INNER JOIN POSTS AS T2 ON T1.POSTID = T2.ID WHERE T1.USERID = 1465 ORDER BY T2.FAVORITECOUNT DESC LIMIT 1;
