-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE
);
INSERT INTO EXAMINATION VALUES (0, '2200-12-01');
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE,
	ADMISSION VARCHAR(20)
);
INSERT INTO PATIENT VALUES (0, '1400-10-01', '');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-02-01', 0, '');
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT PATIENT.ID) FROM PATIENT LEFT JOIN EXAMINATION ON PATIENT.ID = EXAMINATION.ID WHERE EXAMINATION.EXAMINATION_DATE IS NULL OR (STRFTIME('%Y', EXAMINATION.EXAMINATION_DATE) - STRFTIME('%Y', PATIENT.FIRST_DATE)) >= 1;
-- ----------sql2------------
-- 0
SELECT COUNT(DISTINCT T1.ID) FROM PATIENT AS T1 INNER JOIN EXAMINATION AS T2 ON T1.ID = T2.ID WHERE T1.ADMISSION = '+' AND STRFTIME('%Y', T2.EXAMINATION_DATE) - STRFTIME('%Y', T1.FIRST_DATE) >= 1;
