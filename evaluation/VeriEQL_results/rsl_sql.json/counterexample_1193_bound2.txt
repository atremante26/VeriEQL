-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	ANA INTEGER,
	ANA_PATTERN VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (-1, 0, 'm');
INSERT INTO EXAMINATION VALUES (-1, 0, 'jk');
CREATE TABLE PATIENT (
	ID INTEGER,
	SEX VARCHAR(20),
	BIRTHDAY DATE
);
INSERT INTO PATIENT VALUES (-1, 'F', '1980-01-01');
INSERT INTO PATIENT VALUES (0, 'l', '1000-03-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-12-01', 0, '');
INSERT INTO LABORATORY VALUES (-1, '1001-03-30', 0, '');
-- ----------sql1------------
-- 2
SELECT COUNT(*) FROM PATIENT AS P INNER JOIN EXAMINATION AS E ON P.ID = E.ID WHERE E.ANA_PATTERN != 'P' AND P.SEX = 'F' AND P.BIRTHDAY BETWEEN '1980-01-01' AND '1989-12-31';
-- ----------sql2------------
-- 1
SELECT COUNT(DISTINCT T1.ID) FROM PATIENT AS T1 INNER JOIN EXAMINATION AS T2 ON T1.ID = T2.ID WHERE T2.ANA_PATTERN != 'P' AND STRFTIME('%Y', T1.BIRTHDAY) BETWEEN '1980' AND '1989' AND T1.SEX = 'F';
