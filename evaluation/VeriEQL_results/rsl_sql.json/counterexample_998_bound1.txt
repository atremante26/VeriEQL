-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER
);
INSERT INTO CIRCUITS VALUES (0);
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORS VALUES (0);
CREATE TABLE DRIVERS (
	DRIVERID INTEGER
);
INSERT INTO DRIVERS VALUES (0);
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (0);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER
);
INSERT INTO RACES VALUES (0, 0, 0);
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POINTS INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 0, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POINTS INTEGER,
	POSITION INTEGER,
	WINS INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, 0, 0, 0, 0, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	POINTS INTEGER,
	POSITION INTEGER,
	WINS INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (0, 0, 0, 91, 92, NULL);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER,
	POSITION INTEGER
);
INSERT INTO LAPTIMES VALUES (0, 0, 0, 0);
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER
);
INSERT INTO PITSTOPS VALUES (0, 0, 0, 0);
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER
);
INSERT INTO QUALIFYING VALUES (0, 0, 0, 0, 0);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER,
	POINTS INTEGER,
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (0, 0, 0, 0, 0, 0, 0);
-- ----------sql1------------
SELECT WINS FROM DRIVERSTANDINGS WHERE POSITION = 91;
-- ----------sql2------------
-- NULL
SELECT SUM(CASE WHEN POINTS = 91 THEN WINS ELSE 0 END) FROM DRIVERSTANDINGS;
