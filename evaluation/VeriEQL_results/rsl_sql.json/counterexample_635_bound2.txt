-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (-1, -1, '2147483648');
INSERT INTO BADGES VALUES (0, 0, '2147483648');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (1, 5, -1);
INSERT INTO COMMENTS VALUES (0, 5, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 5, 0);
INSERT INTO POSTHISTORY VALUES (-1, 4, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, 4, 4);
INSERT INTO POSTLINKS VALUES (1, 4, 4);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (5, -1, -1, 5);
INSERT INTO POSTS VALUES (4, 0, -1, 5);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 5);
INSERT INTO TAGS VALUES (1, 0, 4);
CREATE TABLE USERS (
	ID INTEGER,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, 'MATT PARKER');
INSERT INTO USERS VALUES (0, 'MATT PARKER');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 5, 0);
INSERT INTO VOTES VALUES (-1, 5, -1);
-- ----------sql1------------
-- 2
SELECT COUNT(POSTS.ID) FROM POSTS JOIN USERS ON POSTS.OWNERUSERID = USERS.ID JOIN VOTES ON POSTS.ID = VOTES.POSTID WHERE USERS.DISPLAYNAME = 'MATT PARKER' AND VOTES.POSTID > 4;
-- ----------sql2------------
SELECT COUNT(T1.ID) FROM USERS AS T1 INNER JOIN POSTHISTORY AS T2 ON T1.ID = T2.USERID INNER JOIN POSTS AS T3 ON T2.POSTID = T3.ID INNER JOIN VOTES AS T4 ON T4.POSTID = T3.ID WHERE T1.DISPLAYNAME = 'MATT PARKER' GROUP BY T2.POSTID, T4.ID HAVING COUNT(T4.ID) > 4;
