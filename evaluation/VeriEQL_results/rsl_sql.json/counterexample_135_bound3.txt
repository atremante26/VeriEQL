-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20)
);
INSERT INTO ACCOUNT VALUES (-1, 0, 'POPLATEK MESICNE');
INSERT INTO ACCOUNT VALUES (0, -1, 'POPLATEK MESICNE');
INSERT INTO ACCOUNT VALUES (-2, 0, '2147483648');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (-1, 1);
INSERT INTO CARD VALUES (-2, 0);
INSERT INTO CARD VALUES (0, 2);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (1, 1);
INSERT INTO CLIENT VALUES (0, -1);
INSERT INTO CLIENT VALUES (2, -1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (2, 0, -2);
INSERT INTO DISP VALUES (1, 1, -1);
INSERT INTO DISP VALUES (0, 1, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (-1);
INSERT INTO DISTRICT VALUES (0);
INSERT INTO DISTRICT VALUES (1);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (2, 0);
INSERT INTO LOAN VALUES (0, -2);
INSERT INTO LOAN VALUES (1, -1);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (2, -2);
INSERT INTO ORDER VALUES (1, -2);
INSERT INTO ORDER VALUES (0, -2);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	OPERATION VARCHAR(20),
	BALANCE INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, -1, 'VYBER KARTOU', -1, 0);
INSERT INTO TRANS VALUES (1, 0, 'VYBER KARTOU', -1, 0);
INSERT INTO TRANS VALUES (-1, 0, 'VYBER KARTOU', -1, 0);
-- ----------sql1------------
-- 2
SELECT COUNT(DISTINCT ACCOUNT.ACCOUNT_ID) FROM ACCOUNT INNER JOIN TRANS ON ACCOUNT.ACCOUNT_ID = TRANS.ACCOUNT_ID INNER JOIN DISP ON ACCOUNT.ACCOUNT_ID = DISP.ACCOUNT_ID INNER JOIN CARD ON DISP.DISP_ID = CARD.DISP_ID WHERE TRANS.OPERATION = 'VYBER KARTOU' AND ACCOUNT.FREQUENCY = 'POPLATEK MESICNE' AND TRANS.BALANCE < 0;
-- ----------sql2------------
-- 3
SELECT COUNT(T1.ACCOUNT_ID) FROM TRANS AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T1.BALANCE < 0 AND T1.OPERATION = 'VYBER KARTOU' AND T2.FREQUENCY = 'POPLATEK MESICNE';
