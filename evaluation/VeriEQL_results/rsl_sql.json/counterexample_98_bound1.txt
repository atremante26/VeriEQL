-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, 0, 'POPLATEK TYDNE', '1000-10-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER
);
INSERT INTO LOAN VALUES (0, 0, '1997-03-01', NULL);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	AMOUNT INTEGER
);
INSERT INTO ORDER VALUES (0, 0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, '1000-01-01', 0, 0);
-- ----------sql1------------
SELECT A.ACCOUNT_ID FROM ACCOUNT AS A INNER JOIN LOAN AS L ON A.ACCOUNT_ID = L.ACCOUNT_ID WHERE STRFTIME('%Y', L.DATE) = '1997' AND A.FREQUENCY = 'POPLATEK TYDNE' AND L.AMOUNT = (SELECT MIN(AMOUNT) FROM LOAN AS L2 WHERE STRFTIME('%Y', L2.DATE) = '1997' AND L2.ACCOUNT_ID = L.ACCOUNT_ID);
-- ----------sql2------------
-- 0
SELECT T2.ACCOUNT_ID FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE STRFTIME('%Y', T1.DATE) = '1997' AND T2.FREQUENCY = 'POPLATEK TYDNE' ORDER BY T1.AMOUNT LIMIT 1;
