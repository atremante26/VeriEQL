-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (2, 'Good');
INSERT INTO ALIGNMENT VALUES (1, 'Good');
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (2, 'Intelligence');
INSERT INTO ATTRIBUTE VALUES (1, 'Intelligence');
CREATE TABLE COLOUR (
	ID INTEGER,
	COLOUR VARCHAR(20)
);
INSERT INTO COLOUR VALUES (4, '2147483648');
INSERT INTO COLOUR VALUES (33, '2147483648');
CREATE TABLE GENDER (
	ID INTEGER,
	GENDER VARCHAR(20)
);
INSERT INTO GENDER VALUES (3, 'Female');
INSERT INTO GENDER VALUES (2, 'N/A');
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (3, 'Marvel Comics');
INSERT INTO PUBLISHER VALUES (1, 'IDW Publishing');
CREATE TABLE RACE (
	ID INTEGER,
	RACE VARCHAR(20)
);
INSERT INTO RACE VALUES (2, '2147483648');
INSERT INTO RACE VALUES (1, '2147483648');
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	FULL_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER,
	HEIGHT_CM INTEGER,
	WEIGHT_KG INTEGER
);
INSERT INTO SUPERHERO VALUES (2, '2147483648', '2147483648', 2, 33, 4, 4, 2, 1, 1, 0, 1);
INSERT INTO SUPERHERO VALUES (1, '2147483649', '2147483648', 3, 4, 33, 33, 2, 1, 1, 0, 1);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (2, 1, 10);
INSERT INTO HERO_ATTRIBUTE VALUES (2, 1, 10);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (2, '2147483648');
INSERT INTO SUPERPOWER VALUES (1, '2147483648');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (2, 2);
INSERT INTO HERO_POWER VALUES (2, 1);
-- ----------sql1------------
-- 0
SELECT COUNT(CASE WHEN COLOUR.ID = 7 THEN 1 ELSE NULL END) - COUNT(CASE WHEN COLOUR.ID = 1 THEN 1 ELSE NULL END) FROM SUPERHERO INNER JOIN COLOUR ON SUPERHERO.EYE_COLOUR_ID = COLOUR.ID WHERE SUPERHERO.WEIGHT_KG = 0 OR SUPERHERO.WEIGHT_KG IS NULL;
-- ----------sql2------------
-- NULL
SELECT SUM(CASE WHEN T2.ID = 7 THEN 1 ELSE 0 END) - SUM(CASE WHEN T2.ID = 1 THEN 1 ELSE 0 END) FROM SUPERHERO AS T1 INNER JOIN COLOUR AS T2 ON T1.EYE_COLOUR_ID = T2.ID WHERE T1.WEIGHT_KG = 0 OR T1.WEIGHT_KG IS NULL;
