-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (1, 'Bad');
INSERT INTO ALIGNMENT VALUES (2, 'Bad');
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (2, 'Durability');
INSERT INTO ATTRIBUTE VALUES (1, 'Combat');
CREATE TABLE COLOUR (
	ID INTEGER,
	COLOUR VARCHAR(20)
);
INSERT INTO COLOUR VALUES (12, '2147483648');
INSERT INTO COLOUR VALUES (13, '2147483648');
CREATE TABLE GENDER (
	ID INTEGER,
	GENDER VARCHAR(20)
);
INSERT INTO GENDER VALUES (1, 'Female');
INSERT INTO GENDER VALUES (3, 'N/A');
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (11, 'Marvel Comics');
INSERT INTO PUBLISHER VALUES (12, 'Titan Books');
CREATE TABLE RACE (
	ID INTEGER,
	RACE VARCHAR(20)
);
INSERT INTO RACE VALUES (1, '2147483648');
INSERT INTO RACE VALUES (2, '2147483648');
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	FULL_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER,
	HEIGHT_CM INTEGER,
	WEIGHT_KG INTEGER
);
INSERT INTO SUPERHERO VALUES (2, '2147483648', '2147483648', 3, 12, 12, 13, 1, 11, 2, 0, 0);
INSERT INTO SUPERHERO VALUES (1, '2147483649', '2147483648', 1, 13, 12, 12, 1, 11, 1, 0, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (2, 1, 10);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 1, 10);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (2, '2147483648');
INSERT INTO SUPERPOWER VALUES (1, '2147483648');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (1, 1);
INSERT INTO HERO_POWER VALUES (2, 1);
-- ----------sql1------------
-- 0, 0
SELECT CAST(COUNT(CASE WHEN ALIGNMENT.ALIGNMENT = 'BAD' THEN 1 ELSE NULL END) AS REAL) * 100 / COUNT(SUPERHERO.ID) AS PERCENTAGE, COUNT(CASE WHEN ALIGNMENT.ALIGNMENT = 'BAD' AND PUBLISHER.PUBLISHER_NAME = 'MARVEL COMICS' THEN 1 ELSE NULL END) AS MARVEL_COUNT FROM SUPERHERO INNER JOIN ALIGNMENT ON SUPERHERO.ALIGNMENT_ID = ALIGNMENT.ID INNER JOIN PUBLISHER ON SUPERHERO.PUBLISHER_ID = PUBLISHER.ID;
-- ----------sql2------------
-- 0, NULL
SELECT (CAST(COUNT(*) AS REAL) * 100 / (SELECT COUNT(*) FROM SUPERHERO)), CAST(SUM(CASE WHEN T2.PUBLISHER_NAME = 'MARVEL COMICS' THEN 1 ELSE 0 END) AS REAL) FROM SUPERHERO AS T1 INNER JOIN PUBLISHER AS T2 ON T1.PUBLISHER_ID = T2.ID INNER JOIN ALIGNMENT AS T3 ON T3.ID = T1.ALIGNMENT_ID WHERE T3.ALIGNMENT = 'BAD';
