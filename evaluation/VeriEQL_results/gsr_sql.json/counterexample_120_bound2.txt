-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (1, -1, '', '2800-01-31');
INSERT INTO ACCOUNT VALUES (0, -1, '', '2000-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO CARD VALUES (0, -1, '');
INSERT INTO CARD VALUES (1, -1, '');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (-1, 0);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (-1, -1, 0, '');
INSERT INTO DISP VALUES (0, 0, 0, '');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A3 VARCHAR(20)
);
INSERT INTO DISTRICT VALUES (-1, 'EAST BOHEMIA');
INSERT INTO DISTRICT VALUES (0, 'ÐÒÔ');
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE
);
INSERT INTO LOAN VALUES (0, 1, '1000-12-01');
INSERT INTO LOAN VALUES (1, 0, '1000-06-01');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
INSERT INTO ORDER VALUES (1, 1);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	TYPE VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 1, '1000-10-01', '', 0);
INSERT INTO TRANS VALUES (-1, 1, '1000-05-01', '', 0);
-- ----------sql1------------
SELECT ACCOUNT.ACCOUNT_ID, ACCOUNT.FREQUENCY FROM ACCOUNT JOIN DISP ON ACCOUNT.ACCOUNT_ID = DISP.ACCOUNT_ID JOIN CLIENT ON DISP.CLIENT_ID = CLIENT.CLIENT_ID JOIN DISTRICT ON ACCOUNT.DISTRICT_ID = DISTRICT.DISTRICT_ID WHERE DISTRICT.A3 = 'EAST BOHEMIA' AND ACCOUNT.DATE BETWEEN '1995-01-01' AND '2000-12-31' AND DISP.TYPE = 'OWNER';
-- ----------sql2------------
-- 0, 
SELECT T1.ACCOUNT_ID, T1.FREQUENCY FROM ACCOUNT AS T1 INNER JOIN DISTRICT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE T2.A3 = 'EAST BOHEMIA' AND STRFTIME('%Y', T1.DATE) BETWEEN '1995' AND '2000';
