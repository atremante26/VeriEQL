-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (0, '');
INSERT INTO EXAMINATION VALUES (1, '');
CREATE TABLE PATIENT (
	ID INTEGER,
	BIRTHDAY DATE,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (1, '1985-01-31', 'c');
INSERT INTO PATIENT VALUES (0, '8000-03-31', '');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (1, '1000-12-01', '-', '');
INSERT INTO LABORATORY VALUES (0, '1000-07-30', '-', '');
-- ----------sql1------------
-- 
SELECT DISTINCT P.DIAGNOSIS FROM PATIENT P JOIN LABORATORY L ON P.ID = L.ID WHERE STRFTIME('%Y', P.BIRTHDAY) > '1985' AND L.RA IN ('-', '+-');
-- ----------sql2------------
-- c
-- 
SELECT T1.DIAGNOSIS FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE (T2.RA = '-' OR T2.RA = '+-') AND T1.BIRTHDAY > '1985-01-01';
