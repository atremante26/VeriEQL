-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (0, 0);
INSERT INTO ACCOUNT VALUES (-1, -1);
INSERT INTO ACCOUNT VALUES (-2, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO CARD VALUES (-1, 0, NULL);
INSERT INTO CARD VALUES (-2, 0, NULL);
INSERT INTO CARD VALUES (0, 0, NULL);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (-1, 0);
INSERT INTO CLIENT VALUES (-2, 0);
INSERT INTO CLIENT VALUES (0, 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (0, -1, 0, NULL);
INSERT INTO DISP VALUES (1, 0, -2, '2147483648');
INSERT INTO DISP VALUES (-1, 0, -1, '2147483648');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A3 VARCHAR(20)
);
INSERT INTO DISTRICT VALUES (0, 'SOUTH BOHEMIA');
INSERT INTO DISTRICT VALUES (-1, 'SOUTH BOHEMIA');
INSERT INTO DISTRICT VALUES (1, '2147483648');
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (-1, -1);
INSERT INTO LOAN VALUES (0, 0);
INSERT INTO LOAN VALUES (1, -1);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
INSERT INTO ORDER VALUES (1, -2);
INSERT INTO ORDER VALUES (-1, -1);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (-2, -2, '2147483648', 0);
INSERT INTO TRANS VALUES (-1, 0, '2147483648', 0);
INSERT INTO TRANS VALUES (0, 0, '2147483648', 0);
-- ----------sql1------------
-- 2
SELECT COUNT(DISTINCT ACCOUNT.ACCOUNT_ID) FROM ACCOUNT JOIN DISP ON ACCOUNT.ACCOUNT_ID = DISP.ACCOUNT_ID JOIN CLIENT ON DISP.CLIENT_ID = CLIENT.CLIENT_ID JOIN DISTRICT ON ACCOUNT.DISTRICT_ID = DISTRICT.DISTRICT_ID LEFT JOIN CARD ON DISP.DISP_ID = CARD.DISP_ID WHERE DISTRICT.A3 = 'SOUTH BOHEMIA' AND CARD.CARD_ID IS NULL;
-- ----------sql2------------
-- 0
SELECT COUNT(T3.ACCOUNT_ID) FROM DISTRICT AS T1 INNER JOIN CLIENT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID INNER JOIN DISP AS T3 ON T2.CLIENT_ID = T3.CLIENT_ID WHERE T1.A3 = 'SOUTH BOHEMIA' AND T3.TYPE != 'OWNER';
