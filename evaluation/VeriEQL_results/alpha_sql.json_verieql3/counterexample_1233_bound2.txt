-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER,
	DESCRIPTION DATE,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (0, '1991-11-01', '1991-12-01');
INSERT INTO PATIENT VALUES (1, '1000-05-01', '1991-01-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	GLU INTEGER,
	PT INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-07-01', 0, 27, '', '');
INSERT INTO LABORATORY VALUES (1, '1001-01-01', 0, 28, '', '');
-- ----------sql1------------
-- 0, 0
SELECT DISTINCT PATIENT.ID, LABORATORY.GLU FROM PATIENT INNER JOIN LABORATORY ON PATIENT.ID = LABORATORY.ID WHERE STRFTIME('%Y', PATIENT.DESCRIPTION) = '1991' AND LABORATORY.GLU < 180;
-- ----------sql2------------
-- 0, 0
-- 1, 0
SELECT DISTINCT T1.ID, T2.GLU FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE STRFTIME('%Y', T1.FIRST_DATE) = '1991' AND T2.GLU < 180;
