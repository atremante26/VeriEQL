-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (1);
INSERT INTO EXAMINATION VALUES (1);
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (0, '8200-12-01');
INSERT INTO PATIENT VALUES (1, '4348-01-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	IGA INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-12-01', 0, 80, '', '');
INSERT INTO LABORATORY VALUES (1, '1000-03-30', 0, 79, '', '');
-- ----------sql1------------
-- 0
SELECT COUNT(DISTINCT PATIENT.ID) FROM LABORATORY INNER JOIN PATIENT ON LABORATORY.ID = PATIENT.ID WHERE LABORATORY.IGA > 80 AND LABORATORY.IGA < 500 AND STRFTIME('%Y', PATIENT.FIRST_DATE) >= '1990';
-- ----------sql2------------
-- 1
SELECT COUNT(T1.ID) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.IGA BETWEEN 80 AND 500 AND  STRFTIME('%Y',  T1.FIRST_DATE) > '1990';
