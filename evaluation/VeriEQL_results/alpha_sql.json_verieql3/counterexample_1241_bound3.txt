-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (-1);
INSERT INTO EXAMINATION VALUES (-2);
INSERT INTO EXAMINATION VALUES (-2);
CREATE TABLE PATIENT (
	ID INTEGER
);
INSERT INTO PATIENT VALUES (-2);
INSERT INTO PATIENT VALUES (-1);
INSERT INTO PATIENT VALUES (0);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	PLT INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-01-03', 401, '2147483648');
INSERT INTO LABORATORY VALUES (-2, '1000-01-01', 100, '2147483648');
INSERT INTO LABORATORY VALUES (-1, '1000-01-02', 401, '2147483648');
-- ----------sql1------------
-- -2
SELECT SUM(CASE WHEN LABORATORY.PLT < 100 THEN 1 ELSE 0 END) - SUM(CASE WHEN LABORATORY.PLT > 400 THEN 1 ELSE 0 END) AS DIFFERENCE FROM LABORATORY WHERE LABORATORY.PLT <= 100 OR LABORATORY.PLT >= 400;
-- ----------sql2------------
-- -1
SELECT SUM(CASE WHEN T2.PLT <= 100 THEN 1 ELSE 0 END) - SUM(CASE WHEN T2.PLT >= 400 THEN 1 ELSE 0 END) FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID;
