-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-2, 2);
INSERT INTO BADGES VALUES (-1, 1);
INSERT INTO BADGES VALUES (0, 1);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-2, 4, 1);
INSERT INTO COMMENTS VALUES (-1, 4, 1);
INSERT INTO COMMENTS VALUES (0, 4, 1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, 4, 1);
INSERT INTO POSTHISTORY VALUES (0, 4, 1);
INSERT INTO POSTHISTORY VALUES (1, 4, 1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-2, 2, 4);
INSERT INTO POSTLINKS VALUES (-1, 2, 4);
INSERT INTO POSTLINKS VALUES (0, 4, 4);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (3, 1, 1, 4);
INSERT INTO POSTS VALUES (2, 1, 1, 4);
INSERT INTO POSTS VALUES (4, 1, 1, 4);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, 4);
INSERT INTO TAGS VALUES (0, 0, 4);
INSERT INTO TAGS VALUES (1, 0, 4);
CREATE TABLE USERS (
	ID INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (0, 1);
INSERT INTO USERS VALUES (2, NULL);
INSERT INTO USERS VALUES (1, NULL);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 4, 0);
INSERT INTO VOTES VALUES (0, 4, 0);
INSERT INTO VOTES VALUES (-1, 4, 0);
-- ----------sql1------------
-- 3
SELECT AVG(VOTE_COUNT) FROM (SELECT COUNT(T2.POSTID) AS VOTE_COUNT FROM USERS AS T1 INNER JOIN VOTES AS T2 ON T1.ID = T2.USERID WHERE T1.AGE = (SELECT MAX(AGE) FROM USERS) GROUP BY T1.ID) AS VOTE_COUNTS;
-- ----------sql2------------
-- 4
SELECT AVG(POSTID) FROM VOTES WHERE USERID IN ( SELECT ID FROM USERS WHERE AGE = ( SELECT MAX(AGE) FROM USERS ) );
