-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE,
	ACL_IGG INTEGER,
	ACL_IGM INTEGER,
	ANA INTEGER,
	ANA_PATTERN VARCHAR(20),
	ACL_IGA INTEGER,
	DIAGNOSIS VARCHAR(20),
	KCT VARCHAR(20),
	RVVT VARCHAR(20),
	LAC VARCHAR(20),
	SYMPTOMS VARCHAR(20),
	THROMBOSIS INTEGER
);
INSERT INTO EXAMINATION VALUES (27654, '0001-01-01', 0, 0, 0, 'S,N', 0, '2147483648', '+', '+', '+', '2147483648', 0);
INSERT INTO EXAMINATION VALUES (27655, '0001-01-01', 0, 0, 0, 'S,P', 0, '2147483648', '+', '+', '+', '2147483649', 1);
CREATE TABLE PATIENT (
	ID INTEGER,
	SEX VARCHAR(20),
	BIRTHDAY DATE,
	DESCRIPTION DATE,
	FIRST_DATE DATE,
	ADMISSION VARCHAR(20),
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (27655, 'F', '0001-01-01', '0001-01-01', '0001-01-01', '+(', 'SLE');
INSERT INTO PATIENT VALUES (27654, 'F', '0001-01-01', '0001-01-01', '0001-01-01', '+', 'SLE');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	GOT INTEGER,
	GPT INTEGER,
	LDH INTEGER,
	ALP INTEGER,
	TP INTEGER,
	ALB INTEGER,
	UA INTEGER,
	UN INTEGER,
	CRE INTEGER,
	T_BIL INTEGER,
	T_CHO INTEGER,
	TG INTEGER,
	CPK INTEGER,
	GLU INTEGER,
	WBC INTEGER,
	RBC INTEGER,
	HGB INTEGER,
	HCT INTEGER,
	PLT INTEGER,
	PT INTEGER,
	APTT INTEGER,
	FG INTEGER,
	PIC INTEGER,
	TAT INTEGER,
	TAT2 INTEGER,
	U_PRO VARCHAR(20),
	IGG INTEGER,
	IGA INTEGER,
	IGM INTEGER,
	CRP VARCHAR(20),
	RA VARCHAR(20),
	RF VARCHAR(20),
	C3 INTEGER,
	C4 INTEGER,
	RNP VARCHAR(20),
	SM VARCHAR(20),
	SC170 VARCHAR(20),
	SSA VARCHAR(20),
	SSB VARCHAR(20),
	CENTROMEA VARCHAR(20),
	DNA VARCHAR(20),
	DNA_II INTEGER
);
INSERT INTO LABORATORY VALUES (27655, '0001-01-01', 3, 1, 25, 11, 0, 1, 1, 0, 1, 1, 37, 1, 0, 62, 1, 1, 2, 3, 5, 11, 57, 24, 114, 63, 59, '>=1000', 3, 1, 0, '2147483648', '+', '2147483648', 15, 3, '15', '8', 'negative', 'negative', 'negative', 'negative', '2147483648', -2147483648);
INSERT INTO LABORATORY VALUES (27654, '0001-01-02', 3, 2, 26, 11, 0, 2, 2, 0, 1, 1, 38, 2, 1, 63, 2, 1, 3, 4, 6, 12, 60, 25, 115, 64, 60, '3', 4, 2, 1, '2147483648', '+-', '2147483649', 15, 3, '0', 'negative', 'negative', 'negative', '32', 'negative', '2147483649', -2147483648);
-- ----------sql1------------
-- 0
SELECT CAST(COUNT(CASE WHEN ADMISSION = '-' THEN 1 END) AS REAL) / COUNT(CASE WHEN ADMISSION = '+' THEN 1 END) AS OUTPATIENT_TO_INPATIENT_RATIO FROM PATIENT WHERE DIAGNOSIS = 'SLE';
-- ----------sql2------------
-- NULL
SELECT SUM(CASE WHEN ADMISSION = '+' THEN 1.0 ELSE 0 END) / SUM(CASE WHEN ADMISSION = '-' THEN 1 ELSE 0 END) FROM PATIENT WHERE DIAGNOSIS = 'SLE';
