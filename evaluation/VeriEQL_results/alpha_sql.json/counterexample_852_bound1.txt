-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER,
	COUNTRY VARCHAR(20)
);
INSERT INTO CIRCUITS VALUES (0, 'JAPAN');
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORS VALUES (0);
CREATE TABLE DRIVERS (
	DRIVERID INTEGER
);
INSERT INTO DRIVERS VALUES (0);
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (2010);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER
);
INSERT INTO RACES VALUES (0, 2010, 0);
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, 0, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (0, 0, 0);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER
);
INSERT INTO LAPTIMES VALUES (0, 0, 0);
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER
);
INSERT INTO PITSTOPS VALUES (0, 0, 0, 0);
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO QUALIFYING VALUES (0, 0, 0, 0);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (0, 0, 0, 0, 0);
-- ----------sql1------------
-- 1
SELECT COUNT(*) FROM RACES AS R INNER JOIN CIRCUITS AS C ON R.CIRCUITID = C.CIRCUITID WHERE R.YEAR = 2010 AND NOT C.COUNTRY IN ('ASIA', 'EUROPE');
-- ----------sql2------------
-- 0
SELECT COUNT(T3.RACEID) FROM CIRCUITS AS T1 INNER JOIN RACES AS T3 ON T3.CIRCUITID = T1.CIRCUITID WHERE T1.COUNTRY NOT IN ( 'BAHRAIN', 'CHINA', 'SINGAPORE', 'JAPAN', 'KOREA', 'TURKEY', 'UAE', 'MALAYSIA', 'SPAIN', 'MONACO', 'AZERBAIJAN', 'AUSTRIA', 'BELGIUM', 'FRANCE', 'GERMANY', 'HUNGARY', 'ITALY', 'UK' ) AND T3.YEAR = 2010;
