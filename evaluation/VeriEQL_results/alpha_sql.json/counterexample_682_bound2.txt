-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20),
	DATE DATE
);
INSERT INTO BADGES VALUES (0, 0, '', '1000-01-01');
INSERT INTO BADGES VALUES (1, 0, '', '1000-03-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, '1000-09-01', 0);
INSERT INTO COMMENTS VALUES (1, 0, '1000-09-01', 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 0, '1000-02-01', 0);
INSERT INTO POSTHISTORY VALUES (1, 0, '1000-01-01', 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	CREATIONDATE DATE,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, '1000-01-01', 0, 0);
INSERT INTO POSTLINKS VALUES (1, '1000-07-01', 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	FAVORITECOUNT INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER,
	OWNERDISPLAYNAME VARCHAR(20)
);
INSERT INTO POSTS VALUES (0, 1, -1, 1, 1, '');
INSERT INTO POSTS VALUES (1, 0, 0, 1, 1, '');
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 0);
INSERT INTO TAGS VALUES (1, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	CREATIONDATE DATE,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (1, '2010-03-01', '');
INSERT INTO USERS VALUES (0, '2010-01-01', 'A');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-1, 0, '1000-01-01', 1);
INSERT INTO VOTES VALUES (0, 0, '1000-01-01', 1);
-- ----------sql1------------
-- 1, 
SELECT ID, OWNERDISPLAYNAME FROM POSTS WHERE FAVORITECOUNT = (SELECT MAX(FAVORITECOUNT) FROM POSTS);
-- ----------sql2------------
-- 0, A
SELECT T2.OWNERUSERID, T1.DISPLAYNAME FROM USERS AS T1 INNER JOIN POSTS AS T2 ON T1.ID = T2.OWNERUSERID WHERE STRFTIME('%Y', T1.CREATIONDATE) = '2010' ORDER BY T2.FAVORITECOUNT DESC LIMIT 1;
