-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	DATE DATE
);
INSERT INTO BADGES VALUES (0, 1, '1000-08-01');
INSERT INTO BADGES VALUES (1, 1, '1000-05-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, -1, '1000-04-01', 1);
INSERT INTO COMMENTS VALUES (1, -1, '1000-05-01', 1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, -1, '1000-01-01', 0);
INSERT INTO POSTHISTORY VALUES (1, -1, '1000-01-01', 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	CREATIONDATE DATE,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, '1000-10-01', -1, -1);
INSERT INTO POSTLINKS VALUES (1, '1000-07-01', -1, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-1, 0, 0, -1);
INSERT INTO POSTS VALUES (0, 0, 0, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, 0);
INSERT INTO TAGS VALUES (0, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	CREATIONDATE DATE
);
INSERT INTO USERS VALUES (1, '1000-01-01');
INSERT INTO USERS VALUES (0, '1000-01-01');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER,
	BOUNTYAMOUNT INTEGER
);
INSERT INTO VOTES VALUES (0, 0, '2011-07-01', 1, 50);
INSERT INTO VOTES VALUES (1, 0, '2011-01-01', 1, 50);
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT POSTS.ID) FROM POSTS INNER JOIN VOTES ON POSTS.ID = VOTES.POSTID WHERE STRFTIME('%Y', VOTES.CREATIONDATE) = '2011' AND VOTES.BOUNTYAMOUNT = 50;
-- ----------sql2------------
-- 2
SELECT COUNT(T1.ID) FROM POSTS AS T1 INNER JOIN VOTES AS T2 ON T1.ID = T2.POSTID WHERE T2.BOUNTYAMOUNT = 50 AND STRFTIME('%Y', T2.CREATIONDATE) = '2011';
