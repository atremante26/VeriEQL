-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (0, '1001-05-17');
INSERT INTO PATIENT VALUES (1, '1001-05-17');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	LDH INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1001-05-17', 499, '2147483648');
INSERT INTO LABORATORY VALUES (1, '1001-05-18', 499, '2147483648');
-- ----------sql1------------
-- NULL
SELECT MAX(PATIENT.FIRST_DATE) AS LATEST_FIRST_DATE FROM PATIENT INNER JOIN (SELECT ID, MAX(DATE) AS LATEST_LDH_DATE FROM LABORATORY WHERE ID IN (SELECT ID FROM LABORATORY WHERE LDH >= 500 GROUP BY ID HAVING MAX(LDH) >= 500) GROUP BY ID) AS LAB ON PATIENT.ID = LAB.ID;
-- ----------sql2------------
SELECT T1.FIRST_DATE FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.LDH >= 500 ORDER BY T1.FIRST_DATE DESC LIMIT 1;
