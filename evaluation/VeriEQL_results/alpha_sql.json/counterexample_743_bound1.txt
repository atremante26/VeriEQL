-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (0, '2147483648');
CREATE TABLE ATTRIBUTE (
	ID INTEGER
);
INSERT INTO ATTRIBUTE VALUES (0);
CREATE TABLE COLOUR (
	ID INTEGER
);
INSERT INTO COLOUR VALUES (0);
CREATE TABLE GENDER (
	ID INTEGER
);
INSERT INTO GENDER VALUES (0);
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (0, '2147483648');
CREATE TABLE RACE (
	ID INTEGER
);
INSERT INTO RACE VALUES (0);
CREATE TABLE SUPERHERO (
	ID INTEGER,
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER
);
INSERT INTO SUPERHERO VALUES (0, 0, 0, 0, 0, 0, 0, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (0, 0);
CREATE TABLE SUPERPOWER (
	ID INTEGER
);
INSERT INTO SUPERPOWER VALUES (0);
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (0, 0);
-- ----------sql1------------
-- 0, 0
SELECT CAST(SUM(CASE WHEN A.ALIGNMENT = 'BAD' THEN 1 ELSE 0 END) AS REAL) * 100 / COUNT(S.ID) AS PERCENTAGE_BAD, SUM(CASE WHEN A.ALIGNMENT = 'BAD' AND P.PUBLISHER_NAME = 'MARVEL COMICS' THEN 1 ELSE 0 END) AS COUNT_BAD_MARVEL FROM SUPERHERO AS S INNER JOIN ALIGNMENT AS A ON S.ALIGNMENT_ID = A.ID INNER JOIN PUBLISHER AS P ON S.PUBLISHER_ID = P.ID;
-- ----------sql2------------
-- 0, NULL
SELECT (CAST(COUNT(*) AS REAL) * 100 / (SELECT COUNT(*) FROM SUPERHERO)), CAST(SUM(CASE WHEN T2.PUBLISHER_NAME = 'MARVEL COMICS' THEN 1 ELSE 0 END) AS REAL) FROM SUPERHERO AS T1 INNER JOIN PUBLISHER AS T2 ON T1.PUBLISHER_ID = T2.ID INNER JOIN ALIGNMENT AS T3 ON T3.ID = T1.ALIGNMENT_ID WHERE T3.ALIGNMENT = 'BAD';
