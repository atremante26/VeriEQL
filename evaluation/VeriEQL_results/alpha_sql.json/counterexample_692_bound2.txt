-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20),
	DATE DATE
);
INSERT INTO BADGES VALUES (-1, 0, '', '1000-05-01');
INSERT INTO BADGES VALUES (0, 0, '', '1000-03-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, '1000-01-01', -1);
INSERT INTO COMMENTS VALUES (1, 0, '1000-12-01', -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, 0, '1000-01-01', -1);
INSERT INTO POSTHISTORY VALUES (0, 0, '1000-01-01', -1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	CREATIONDATE DATE,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, '1000-01-01', 0, 0);
INSERT INTO POSTLINKS VALUES (1, '1000-02-01', 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (1, -1, -1, 0);
INSERT INTO POSTS VALUES (0, -1, -1, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 1);
INSERT INTO TAGS VALUES (1, 1);
CREATE TABLE USERS (
	ID INTEGER,
	CREATIONDATE DATE,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, '1000-01-01', '');
INSERT INTO USERS VALUES (0, '1000-01-01', 'ZOLOMON');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 1, '1000-01-01', 0);
INSERT INTO VOTES VALUES (1, 1, '1000-01-01', 0);
-- ----------sql1------------
-- NULL
-- NULL
SELECT STRFTIME('%J', BADGES.DATE) - STRFTIME('%J', USERS.CREATIONDATE) AS DAYSTOGETBADGE FROM BADGES INNER JOIN USERS ON BADGES.USERID = USERS.ID WHERE USERS.DISPLAYNAME = 'ZOLOMON';
-- ----------sql2------------
-- 400
-- 200
SELECT T1.DATE - T2.CREATIONDATE FROM BADGES AS T1 INNER JOIN USERS AS T2 ON T1.USERID = T2.ID WHERE T2.DISPLAYNAME = 'ZOLOMON';
