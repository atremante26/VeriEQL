-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (0, 0, '2147483648');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 0, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	ANSWERCOUNT INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, 0, 0, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (0, '881532782719141326');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 0, 0);
-- ----------sql1------------
SELECT POSTS.ANSWERCOUNT FROM POSTS INNER JOIN USERS ON POSTS.OWNERUSERID = USERS.ID WHERE USERS.DISPLAYNAME = 'CSGILLESPIE' AND POSTS.ANSWERCOUNT = (SELECT MAX(ANSWERCOUNT) FROM POSTS INNER JOIN USERS ON POSTS.OWNERUSERID = USERS.ID WHERE USERS.DISPLAYNAME = 'CSGILLESPIE');
-- ----------sql2------------
-- NULL
SELECT MAX(T1.ANSWERCOUNT) FROM POSTS AS T1 INNER JOIN USERS AS T2 ON T1.OWNERUSERID = T2.ID WHERE T2.DISPLAYNAME = 'CSGILLESPIE';
