-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20),
	ENROLLMENT_K12 INTEGER,
	FRPM_COUNT_K12 INTEGER
);
INSERT INTO FRPM VALUES ('2147483648', 1, 0);
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('2147483648');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	SCHOOL VARCHAR(20),
	SOC VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('2147483648', '2147483648', '2147483648');
-- ----------sql1------------
-- 0
SELECT FRPM_COUNT_K12 / ENROLLMENT_K12 AS ELIGIBLE_FRPM_RATE FROM FRPM ORDER BY FRPM_COUNT_K12 DESC LIMIT 5;
-- ----------sql2------------
SELECT CAST(T1.FRPM_COUNT_K12 AS REAL) / T1.ENROLLMENT_K12 FROM FRPM AS T1 INNER JOIN SCHOOLS AS T2 ON T1.CDSCODE = T2.CDSCODE WHERE T2.SOC = 66 ORDER BY T1.FRPM_COUNT_K12 DESC LIMIT 5;
