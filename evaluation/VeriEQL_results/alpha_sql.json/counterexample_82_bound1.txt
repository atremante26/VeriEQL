-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('2147483648');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('2147483648');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	SCHOOL VARCHAR(20),
	GSOFFERED VARCHAR(20),
	LONGITUDE INTEGER
);
INSERT INTO SCHOOLS VALUES ('2147483648', '2147483648', '2147483648', NULL);
-- ----------sql1------------
SELECT T1.GSOFFERED FROM SCHOOLS AS T1 INNER JOIN (SELECT MAX(ABS(LONGITUDE)) AS MAXABSLONGITUDE FROM SCHOOLS) AS T2 ON ABS(T1.LONGITUDE) = T2.MAXABSLONGITUDE;
-- ----------sql2------------
-- 2147483648
SELECT GSOFFERED FROM SCHOOLS ORDER BY ABS(LONGITUDE) DESC LIMIT 1;
