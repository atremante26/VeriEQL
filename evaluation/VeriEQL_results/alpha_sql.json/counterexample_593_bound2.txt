-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (0, 0, 'SUPPORTER');
INSERT INTO BADGES VALUES (1, -1, NULL);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, -1, -1);
INSERT INTO COMMENTS VALUES (1, -1, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 0, 0);
INSERT INTO POSTHISTORY VALUES (1, 0, -1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, -1, -1);
INSERT INTO POSTLINKS VALUES (-1, -1, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-1, -1, -1, 0);
INSERT INTO POSTS VALUES (0, -1, -1, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, -1);
INSERT INTO TAGS VALUES (0, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	LOCATION VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, '2147483648');
INSERT INTO USERS VALUES (0, 'NEW YORK');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (1, 0, 0);
INSERT INTO VOTES VALUES (0, -1, -1);
-- ----------sql1------------
-- 0
SELECT COUNT(DISTINCT U.ID) AS NUMBEROFUSERS FROM USERS AS U INNER JOIN BADGES AS B1 ON U.ID = B1.USERID AND B1.NAME = 'SUPPORTER' INNER JOIN BADGES AS B2 ON U.ID = B2.USERID AND B2.NAME = 'TEACHER' WHERE U.LOCATION = 'NEW YORK';
-- ----------sql2------------
-- 1
SELECT COUNT(DISTINCT T1.ID) FROM BADGES AS T1 INNER JOIN USERS AS T2 ON T1.USERID = T2.ID WHERE T1.NAME IN ('SUPPORTER', 'TEACHER') AND T2.LOCATION = 'NEW YORK';
