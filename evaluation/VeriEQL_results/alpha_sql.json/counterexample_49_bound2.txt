-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('2147483649');
INSERT INTO FRPM VALUES ('2147483648');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('2147483649');
INSERT INTO SATSCORES VALUES ('2147483648');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20),
	CLOSEDDATE DATE
);
INSERT INTO SCHOOLS VALUES ('2147483649', 'CLOSED', '2147483648', NULL, '1000-01-01');
INSERT INTO SCHOOLS VALUES ('2147483648', 'CLOSED', '2147483649', '2147483648', '1000-01-01');
-- ----------sql1------------
-- 2147483648, NULL, 1
SELECT S.COUNTY, S.SCHOOL, S.CLOSEDDATE FROM SCHOOLS AS S JOIN (SELECT COUNTY, COUNT(CDSCODE) AS CLOSED_COUNT FROM SCHOOLS WHERE STATUSTYPE = 'CLOSED' GROUP BY COUNTY ORDER BY CLOSED_COUNT DESC LIMIT 1) AS TOP_COUNTY ON S.COUNTY = TOP_COUNTY.COUNTY WHERE S.STATUSTYPE = 'CLOSED';
-- ----------sql2------------
-- 2147483649, 2147483648, 1
SELECT DISTINCT COUNTY, SCHOOL, CLOSEDDATE FROM SCHOOLS WHERE COUNTY = ( SELECT COUNTY FROM SCHOOLS WHERE STATUSTYPE = 'CLOSED' GROUP BY COUNTY ORDER BY COUNT(SCHOOL) DESC LIMIT 1 ) AND STATUSTYPE = 'CLOSED' AND SCHOOL IS NOT NULL;
