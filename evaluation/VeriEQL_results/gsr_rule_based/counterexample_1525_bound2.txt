-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CUSTOMERS (
	CUSTOMERID INTEGER,
	SEGMENT VARCHAR(20),
	CURRENCY VARCHAR(20)
);
INSERT INTO CUSTOMERS VALUES (96, 'SME', 'EUR');
INSERT INTO CUSTOMERS VALUES (97, 'KAM', 'CZK');
CREATE TABLE GASSTATIONS (
	GASSTATIONID INTEGER,
	CHAINID INTEGER,
	COUNTRY VARCHAR(20),
	SEGMENT VARCHAR(20)
);
INSERT INTO GASSTATIONS VALUES (44, 1, 'SVK', 'Discount');
INSERT INTO GASSTATIONS VALUES (45, 2, 'SVK', 'Value for money');
CREATE TABLE PRODUCTS (
	PRODUCTID INTEGER,
	DESCRIPTION VARCHAR(20)
);
INSERT INTO PRODUCTS VALUES (2, '2147483648');
INSERT INTO PRODUCTS VALUES (1, '2147483648');
CREATE TABLE TRANSACTIONS_1K (
	TRANSACTIONID INTEGER,
	DATE DATE,
	TIME VARCHAR(20),
	CUSTOMERID INTEGER,
	CARDID INTEGER,
	GASSTATIONID INTEGER,
	PRODUCTID INTEGER,
	AMOUNT INTEGER,
	PRICE INTEGER
);
INSERT INTO TRANSACTIONS_1K VALUES (1, '2012-08-25', '2147483648', 96, 26228, 48, 23, 0, 2);
INSERT INTO TRANSACTIONS_1K VALUES (2, '2012-08-25', '2147483648', 96, 26228, 48, 2, 0, 2);
CREATE TABLE YEARMONTH (
	CUSTOMERID INTEGER,
	DATE VARCHAR(20),
	CONSUMPTION INTEGER
);
INSERT INTO YEARMONTH VALUES (97, '201112', 0);
INSERT INTO YEARMONTH VALUES (97, '201204', 0);
-- ----------sql1------------
-- 50
SELECT (CAST(COUNT(DISTINCT T.CUSTOMERID) AS FLOAT) * 100.0 / CAST((SELECT COUNT(DISTINCT CUSTOMERID) FROM CUSTOMERS) AS FLOAT)) AS PERCENTAGE FROM TRANSACTIONS_1K T JOIN CUSTOMERS C ON T.CUSTOMERID = C.CUSTOMERID WHERE C.CURRENCY = 'EUR' AND T.DATE = '2012-08-25';
-- ----------sql2------------
-- 100
SELECT CAST(SUM(IIF(T2.CURRENCY = 'EUR', 1, 0)) AS FLOAT) * 100 / COUNT(T1.CUSTOMERID) FROM TRANSACTIONS_1K AS T1 INNER JOIN CUSTOMERS AS T2 ON T1.CUSTOMERID = T2.CUSTOMERID WHERE T1.DATE = '2012-08-25';
