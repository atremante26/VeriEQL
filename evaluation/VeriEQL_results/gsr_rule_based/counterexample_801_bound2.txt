-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (2, 'Good');
INSERT INTO ALIGNMENT VALUES (1, 'Good');
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (2, 'Durability');
INSERT INTO ATTRIBUTE VALUES (1, 'Durability');
CREATE TABLE COLOUR (
	ID INTEGER,
	COLOUR VARCHAR(20)
);
INSERT INTO COLOUR VALUES (20, '2147483648');
INSERT INTO COLOUR VALUES (7, '2147483648');
CREATE TABLE GENDER (
	ID INTEGER,
	GENDER VARCHAR(20)
);
INSERT INTO GENDER VALUES (1, 'N/A');
INSERT INTO GENDER VALUES (2, 'N/A');
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (7, 'South Park');
INSERT INTO PUBLISHER VALUES (25, 'J. R. R. Tolkien');
CREATE TABLE RACE (
	ID INTEGER,
	RACE VARCHAR(20)
);
INSERT INTO RACE VALUES (2, '2147483648');
INSERT INTO RACE VALUES (1, '2147483648');
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	FULL_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER,
	HEIGHT_CM INTEGER,
	WEIGHT_KG INTEGER
);
INSERT INTO SUPERHERO VALUES (1, '2147483648', '2147483648', 2, 7, 7, 7, 1, 7, 1, 0, 0);
INSERT INTO SUPERHERO VALUES (2, '2147483649', '2147483648', 1, 7, 7, 20, 2, 7, 1, 0, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 1, 10);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 1, 10);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (1, '2147483648');
INSERT INTO SUPERPOWER VALUES (2, '2147483648');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (1, 2);
INSERT INTO HERO_POWER VALUES (1, 1);
-- ----------sql1------------
-- 1
SELECT (SUM(CASE WHEN GENDER_ID = 1 THEN 1 ELSE 0 END) * 1.0) / SUM(CASE WHEN GENDER_ID = 2 THEN 1 ELSE 0 END) AS RATIO FROM SUPERHERO;
-- ----------sql2------------
-- NULL
SELECT CAST(COUNT(CASE WHEN T2.GENDER = 'MALE' THEN T1.ID ELSE NULL END) AS REAL) / COUNT(CASE WHEN T2.GENDER = 'FEMALE' THEN T1.ID ELSE NULL END) FROM SUPERHERO AS T1 INNER JOIN GENDER AS T2 ON T1.GENDER_ID = T2.ID;
