-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20),
	DATE DATE
);
INSERT INTO BADGES VALUES (-1, 0, 'COMMENTATOR', '2014-12-01');
INSERT INTO BADGES VALUES (0, 0, 'COMMENTATOR', '2014-01-01');
INSERT INTO BADGES VALUES (1, -1, 'B', '2013-02-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, 2, -2);
INSERT INTO COMMENTS VALUES (0, 2, -1);
INSERT INTO COMMENTS VALUES (1, 2, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER,
	COMMENT VARCHAR(20)
);
INSERT INTO POSTHISTORY VALUES (-1, 1, -1, '');
INSERT INTO POSTHISTORY VALUES (0, 2, -1, '');
INSERT INTO POSTHISTORY VALUES (1, 2, -1, '');
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, 2, 2);
INSERT INTO POSTLINKS VALUES (0, 2, 2);
INSERT INTO POSTLINKS VALUES (1, 2, 2);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (1, -2, -2, 2);
INSERT INTO POSTS VALUES (0, -2, -2, 2);
INSERT INTO POSTS VALUES (2, -2, -2, 2);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, 2);
INSERT INTO TAGS VALUES (0, 0, 2);
INSERT INTO TAGS VALUES (1, 0, 2);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (0);
INSERT INTO USERS VALUES (-1);
INSERT INTO USERS VALUES (-2);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-1, 2, -2);
INSERT INTO VOTES VALUES (0, 2, -2);
INSERT INTO VOTES VALUES (1, 2, -2);
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT B.USERID) FROM BADGES AS B WHERE B.NAME = 'COMMENTATOR' AND STRFTIME('%Y', B.DATE) = '2014';
-- ----------sql2------------
-- 2
SELECT COUNT(ID) FROM BADGES WHERE NAME = 'COMMENTATOR' AND STRFTIME('%Y', DATE) = '2014';
