-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-1, 25);
INSERT INTO BADGES VALUES (0, 23);
INSERT INTO BADGES VALUES (1, 24);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-2, 1, 25);
INSERT INTO COMMENTS VALUES (-1, 1, 25);
INSERT INTO COMMENTS VALUES (0, 1, 25);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-2, 1, 25);
INSERT INTO POSTHISTORY VALUES (0, 0, 24);
INSERT INTO POSTHISTORY VALUES (-1, 0, 25);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-2, 1, 1);
INSERT INTO POSTLINKS VALUES (-1, 1, 0);
INSERT INTO POSTLINKS VALUES (0, 1, 1);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 24, 24, 0);
INSERT INTO POSTS VALUES (2, 25, 25, 2);
INSERT INTO POSTS VALUES (1, 24, 25, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (2, 0, 1);
INSERT INTO TAGS VALUES (0, 0, 1);
INSERT INTO TAGS VALUES (1, 0, 1);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (23);
INSERT INTO USERS VALUES (24);
INSERT INTO USERS VALUES (25);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 0, 23);
INSERT INTO VOTES VALUES (-1, 2, 25);
INSERT INTO VOTES VALUES (0, 2, 25);
-- ----------sql1------------
-- 2
SELECT(COUNT(P.ID) * 1.0) / COUNT(V.ID) AS TIMESOFPOSTTHANVOTES FROM POSTS P LEFT JOIN VOTES V ON P.ID = V.POSTID WHERE P.OWNERUSERID = 24;
-- ----------sql2------------
-- NULL
SELECT CAST(COUNT(T2.ID) AS REAL) / COUNT(DISTINCT T1.ID) FROM VOTES AS T1 INNER JOIN POSTS AS T2 ON T1.USERID = T2.OWNERUSERID WHERE T1.USERID = 24;
