-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER
);
INSERT INTO CIRCUITS VALUES (0);
INSERT INTO CIRCUITS VALUES (-1);
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORS VALUES (1);
INSERT INTO CONSTRUCTORS VALUES (0);
CREATE TABLE DRIVERS (
	DRIVERID INTEGER
);
INSERT INTO DRIVERS VALUES (-1);
INSERT INTO DRIVERS VALUES (0);
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (2009);
INSERT INTO SEASONS VALUES (2010);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER,
	TIME VARCHAR(20)
);
INSERT INTO RACES VALUES (-1, 2009, -1, '');
INSERT INTO RACES VALUES (0, 2009, -1, '');
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POINTS INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (1, 0, 1, 0);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 1, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POINTS INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (1, 0, 1, 0);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, -1, 0, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	POINTS INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (-1, 0, -1, 0);
INSERT INTO DRIVERSTANDINGS VALUES (0, 0, -1, 0);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER,
	TIME VARCHAR(20)
);
INSERT INTO LAPTIMES VALUES (-1, -1, 0, '');
INSERT INTO LAPTIMES VALUES (0, 0, 1, '');
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER,
	TIME VARCHAR(20)
);
INSERT INTO PITSTOPS VALUES (-1, -1, 0, 0, '');
INSERT INTO PITSTOPS VALUES (0, 0, 1, 0, '');
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO QUALIFYING VALUES (-1, 0, 0, 1);
INSERT INTO QUALIFYING VALUES (0, 0, 0, 1);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (-1);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POINTS INTEGER,
	TIME VARCHAR(20),
	FASTESTLAP INTEGER,
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (0, -1, 0, 1, 0, 'Q', -2, 0);
INSERT INTO RESULTS VALUES (1, 0, 0, 1, -1, 'T', -1, 0);
-- ----------sql1------------
-- -2
SELECT R.FASTESTLAP FROM RESULTS AS R INNER JOIN RACES AS RA ON R.RACEID = RA.RACEID WHERE RA.YEAR = 2009 ORDER BY R.POINTS DESC LIMIT 1;
-- ----------sql2------------
SELECT T1.FASTESTLAP FROM RESULTS AS T1 INNER JOIN RACES AS T2 ON T1.RACEID = T2.RACEID WHERE T2.YEAR = 2009 AND T1.TIME LIKE '_:%:__.___';
