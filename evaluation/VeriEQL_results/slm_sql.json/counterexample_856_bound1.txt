-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER,
	CIRCUITREF VARCHAR(20),
	NAME VARCHAR(20)
);
INSERT INTO CIRCUITS VALUES (0, 'SEPANG', '2147483648');
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO CONSTRUCTORS VALUES (0, '2147483648');
CREATE TABLE DRIVERS (
	DRIVERID INTEGER
);
INSERT INTO DRIVERS VALUES (0);
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (0);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER,
	NAME VARCHAR(20),
	TIME VARCHAR(20)
);
INSERT INTO RACES VALUES (0, 0, 0, '2147483648', '2147483648');
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, 0, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (0, 0, 0);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER,
	TIME VARCHAR(20)
);
INSERT INTO LAPTIMES VALUES (0, 0, 0, '2147483648');
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER,
	TIME VARCHAR(20)
);
INSERT INTO PITSTOPS VALUES (0, 0, 0, 0, '2147483648');
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO QUALIFYING VALUES (0, 0, 0, 0);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	TIME VARCHAR(20),
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (0, 0, 0, 0, '2147483648', 0);
-- ----------sql1------------
-- 2147483648
SELECT RACES.TIME FROM RACES INNER JOIN CIRCUITS ON RACES.CIRCUITID = CIRCUITS.CIRCUITID WHERE CIRCUITS.CIRCUITREF = 'SEPANG';
-- ----------sql2------------
SELECT DISTINCT T2.TIME FROM CIRCUITS AS T1 INNER JOIN RACES AS T2 ON T2.CIRCUITID = T1.CIRCUITID WHERE T1.NAME = 'SEPANG INTERNATIONAL CIRCUIT';
