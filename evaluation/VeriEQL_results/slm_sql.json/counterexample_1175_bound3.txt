-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (1, '4291-02-01', '');
INSERT INTO EXAMINATION VALUES (1, '4293-02-01', '');
INSERT INTO EXAMINATION VALUES (0, '4292-05-02', '');
CREATE TABLE PATIENT (
	ID INTEGER,
	BIRTHDAY DATE,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (0, '9201-10-01', 'NN');
INSERT INTO PATIENT VALUES (1, '9200-05-01', '');
INSERT INTO PATIENT VALUES (-1, '9201-10-01', '');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	HGB INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '2415-10-31', -4, NULL, 'NNN]');
INSERT INTO LABORATORY VALUES (0, '2416-06-30', -3, 'X', 'Y');
INSERT INTO LABORATORY VALUES (1, '2414-01-31', -2, '', 'NN[');
-- ----------sql1------------
-- -4909, 
SELECT STRFTIME('%Y', E.EXAMINATION_DATE) - STRFTIME('%Y', P.BIRTHDAY) AS AGE, P.DIAGNOSIS FROM EXAMINATION E INNER JOIN PATIENT P ON E.ID = P.ID INNER JOIN LABORATORY L ON P.ID = L.ID ORDER BY L.HGB DESC LIMIT 1;
-- ----------sql2------------
-- -6786, 
SELECT STRFTIME('%Y', T2.DATE) - STRFTIME('%Y', T1.BIRTHDAY), T1.DIAGNOSIS FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID ORDER BY T2.HGB DESC LIMIT 1;
