-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-1, 0);
INSERT INTO BADGES VALUES (0, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, -1, 0);
INSERT INTO COMMENTS VALUES (1, 0, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, -1, 0);
INSERT INTO POSTHISTORY VALUES (1, -1, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, -1, 0);
INSERT INTO POSTLINKS VALUES (0, 0, -1);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	TITLE VARCHAR(20),
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, 'HOW TO TELL IF SOMETHING HAPPENED IN A DATA SET WHICH MONITORS A VALUE OVER TIME', 0, 0);
INSERT INTO POSTS VALUES (-1, 1, '2485540822874262140', 0, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, -1);
INSERT INTO TAGS VALUES (1, 0);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (0);
INSERT INTO USERS VALUES (1);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 0, 0);
INSERT INTO VOTES VALUES (1, -1, 1);
-- ----------sql1------------
-- HOW TO TELL IF SOMETHING HAPPENED IN A DATA SET WHICH MONITORS A VALUE OVER TIME
SELECT POSTS.TITLE AS RELATEDPOSTTITLE FROM POSTS INNER JOIN POSTLINKS ON POSTS.ID = POSTLINKS.POSTID WHERE POSTS.TITLE = 'HOW TO TELL IF SOMETHING HAPPENED IN A DATA SET WHICH MONITORS A VALUE OVER TIME';
-- ----------sql2------------
-- 2485540822874262140
SELECT T3.TITLE FROM POSTLINKS AS T1 INNER JOIN POSTS AS T2 ON T1.POSTID = T2.ID INNER JOIN POSTS AS T3 ON T1.RELATEDPOSTID = T3.ID WHERE T2.TITLE = 'HOW TO TELL IF SOMETHING HAPPENED IN A DATA SET WHICH MONITORS A VALUE OVER TIME';
