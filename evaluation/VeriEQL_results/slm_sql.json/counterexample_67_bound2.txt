-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('');
INSERT INTO FRPM VALUES ('^');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('');
INSERT INTO SATSCORES VALUES ('^');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	DISTRICT VARCHAR(20),
	SCHOOL VARCHAR(20),
	CITY VARCHAR(20),
	CLOSEDDATE DATE,
	DOC VARCHAR(20),
	DOCTYPE VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('^', '', '', 'SAN FRANCISCO', '1989-08-01', '', '');
INSERT INTO SCHOOLS VALUES ('', '', '', '', '1988-01-01', '', '');
-- ----------sql1------------
-- 1
SELECT COUNT(CDSCODE) AS TOTAL_CCD_CLOSURE FROM SCHOOLS WHERE STRFTIME('%Y', CLOSEDDATE) = '1989' AND CITY = 'SAN FRANCISCO';
-- ----------sql2------------
-- 0
SELECT COUNT(SCHOOL) FROM SCHOOLS WHERE STRFTIME('%Y', CLOSEDDATE) = '1989' AND CITY = 'SAN FRANCISCO' AND DOCTYPE = 'COMMUNITY COLLEGE DISTRICT';
