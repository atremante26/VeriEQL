-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (1, 0);
INSERT INTO BADGES VALUES (0, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-1, 61218, 0);
INSERT INTO COMMENTS VALUES (0, 61217, 1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 61218, 1);
INSERT INTO POSTHISTORY VALUES (-1, 61217, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, 61218, 61217);
INSERT INTO POSTLINKS VALUES (0, 61217, 61218);
CREATE TABLE POSTS (
	ID INTEGER,
	VIEWCOUNT INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (61218, 1, 0, 0, 61217);
INSERT INTO POSTS VALUES (61217, 0, 0, 0, 61218);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0, 61218);
INSERT INTO TAGS VALUES (1, 0, 61218);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (0);
INSERT INTO USERS VALUES (1);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 61218, 1);
INSERT INTO VOTES VALUES (1, 61218, 1);
-- ----------sql1------------
-- 1
SELECT POSTS.VIEWCOUNT FROM POSTLINKS INNER JOIN POSTS ON POSTLINKS.POSTID = POSTS.ID WHERE POSTLINKS.RELATEDPOSTID = 61217;
-- ----------sql2------------
-- 0
SELECT T1.VIEWCOUNT FROM POSTS AS T1 INNER JOIN POSTLINKS AS T2 ON T1.ID = T2.POSTID WHERE T2.POSTID = 61217;
