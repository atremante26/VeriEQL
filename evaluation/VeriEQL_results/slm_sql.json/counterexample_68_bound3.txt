-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('');
INSERT INTO FRPM VALUES ('ñòó');
INSERT INTO FRPM VALUES ('¯');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('');
INSERT INTO SATSCORES VALUES ('¯');
INSERT INTO SATSCORES VALUES ('ñòó');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20),
	CLOSEDDATE DATE,
	SOC VARCHAR(20)
);
INSERT INTO SCHOOLS VALUES ('ñòó', '', '', '', '2000-01-30', '-2');
INSERT INTO SCHOOLS VALUES ('', '', 'ÄÖÝéï', 'Â', '2000-01-28', '0');
INSERT INTO SCHOOLS VALUES ('¯', '°±²³´µ¶', '¿', NULL, '1980-12-31', '11');
-- ----------sql1------------
-- ¿
SELECT COUNTY FROM SCHOOLS WHERE SOC = '11' AND CLOSEDDATE BETWEEN '1980-01-01' AND '1989-12-31' GROUP BY COUNTY ORDER BY COUNT(*) DESC LIMIT 1;
-- ----------sql2------------
SELECT COUNTY FROM SCHOOLS WHERE STRFTIME('%Y', CLOSEDDATE) BETWEEN '1980' AND '1989' AND STATUSTYPE = 'CLOSED' AND SOC = 11 GROUP BY COUNTY ORDER BY COUNT(SCHOOL) DESC LIMIT 1;
