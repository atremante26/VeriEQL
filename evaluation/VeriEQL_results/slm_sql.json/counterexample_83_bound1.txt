-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20),
	NSLP_PROVISION_STATUS VARCHAR(20)
);
INSERT INTO FRPM VALUES ('2147483648', '2147483648');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('2147483648');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	SCHOOL VARCHAR(20),
	CITY VARCHAR(20),
	GSOFFERED VARCHAR(20),
	MAGNET INTEGER
);
INSERT INTO SCHOOLS VALUES ('2147483648', '2147483648', '2147483648', '2147483648', 0);
-- ----------sql1------------
-- 0, 0
SELECT COUNT(*) AS TOTALSCHOOLS, COUNT(T1.CDSCODE) AS SCHOOLSWITHMULTIPLEPROVIDING FROM SCHOOLS AS T1 INNER JOIN FRPM AS T2 ON T1.CDSCODE = T2.CDSCODE WHERE T1.MAGNET = 1 AND T2.NSLP_PROVISION_STATUS = 'MULTIPLE PROVISION TYPES' AND T1.GSOFFERED = 'K-8';
-- ----------sql2------------
SELECT T2.CITY, COUNT(T2.CDSCODE) FROM FRPM AS T1 INNER JOIN SCHOOLS AS T2 ON T1.CDSCODE = T2.CDSCODE WHERE T2.MAGNET = 1 AND T2.GSOFFERED = 'K-8' AND T1.NSLP_PROVISION_STATUS = 'MULTIPLE PROVISION TYPES' GROUP BY T2.CITY;
