-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER
);
INSERT INTO PATIENT VALUES (-1);
INSERT INTO PATIENT VALUES (-2);
INSERT INTO PATIENT VALUES (0);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	TP INTEGER,
	ALB INTEGER,
	UN INTEGER,
	RA VARCHAR(20),
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-2, '1997-05-01', 7, 6, 0, '', '');
INSERT INTO LABORATORY VALUES (-1, '1997-09-01', 7, 6, 0, '', '');
INSERT INTO LABORATORY VALUES (0, '1997-01-01', NULL, 6, 0, '', '');
-- ----------sql1------------
-- 2
SELECT COUNT(*) AS COUNT FROM LABORATORY WHERE STRFTIME('%Y', DATE) = '1997' AND TP > 6 AND TP < 8.5;
-- ----------sql2------------
-- 3
SELECT COUNT(ID) FROM LABORATORY WHERE (ALB <= 6.0 OR ALB >= 8.5) AND STRFTIME('%Y', DATE) = '1997';
