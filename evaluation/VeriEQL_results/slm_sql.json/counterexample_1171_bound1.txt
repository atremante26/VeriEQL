-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	EXAMINATION_DATE DATE
);
INSERT INTO EXAMINATION VALUES (0, '1990-05-01');
CREATE TABLE PATIENT (
	ID INTEGER,
	BIRTHDAY DATE
);
INSERT INTO PATIENT VALUES (0, '1000-12-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	RF VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-12-01', 0, '');
-- ----------sql1------------
-- 1
SELECT COUNT(*) FROM EXAMINATION AS E INNER JOIN PATIENT AS P ON E.ID = P.ID WHERE P.BIRTHDAY < '1993-12-31' AND STRFTIME('%Y', E.EXAMINATION_DATE) BETWEEN '1990' AND '1993';
-- ----------sql2------------
-- 0
SELECT COUNT(T1.ID) FROM PATIENT AS T1 INNER JOIN EXAMINATION AS T2 ON T1.ID = T2.ID WHERE STRFTIME('%Y', T2.EXAMINATION_DATE) BETWEEN '1990' AND '1993' AND STRFTIME('%Y', T2.EXAMINATION_DATE) - STRFTIME('%Y', T1.BIRTHDAY) < 18;
