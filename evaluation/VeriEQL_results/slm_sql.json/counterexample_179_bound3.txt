-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (-1, 0, '1000-01-01');
INSERT INTO ACCOUNT VALUES (1, 1, '1000-01-01');
INSERT INTO ACCOUNT VALUES (0, 0, '1000-12-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (-2, 1);
INSERT INTO CARD VALUES (-1, 1);
INSERT INTO CARD VALUES (0, 1);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (617, 2);
INSERT INTO CLIENT VALUES (618, 0);
INSERT INTO CLIENT VALUES (0, 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (2, 617, 0);
INSERT INTO DISP VALUES (0, 617, 1);
INSERT INTO DISP VALUES (1, 618, -1);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (2);
INSERT INTO DISTRICT VALUES (0);
INSERT INTO DISTRICT VALUES (1);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER
);
INSERT INTO LOAN VALUES (-1, -1, '1000-10-01', 0);
INSERT INTO LOAN VALUES (1, 1, '1000-07-01', 0);
INSERT INTO LOAN VALUES (0, 1, '1000-08-01', 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	AMOUNT INTEGER
);
INSERT INTO ORDER VALUES (-2, -1, 0);
INSERT INTO ORDER VALUES (-1, 0, 0);
INSERT INTO ORDER VALUES (0, -1, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 1, '1998-12-01', NULL, NULL);
INSERT INTO TRANS VALUES (-2, 1, '1998-08-01', NULL, NULL);
INSERT INTO TRANS VALUES (-1, 1, '1998-04-01', -11, NULL);
-- ----------sql1------------
-- NULL
SELECT SUM(T.AMOUNT) AS TOTAL_AMOUNT FROM CLIENT AS C INNER JOIN ACCOUNT AS A ON C.DISTRICT_ID = A.DISTRICT_ID INNER JOIN TRANS AS T ON A.ACCOUNT_ID = T.ACCOUNT_ID WHERE C.CLIENT_ID = 617 AND T.DATE LIKE '1998%';
-- ----------sql2------------
-- -11
SELECT SUM(T3.AMOUNT) FROM CLIENT AS T1 INNER JOIN DISP AS T4 ON T1.CLIENT_ID = T4.CLIENT_ID INNER JOIN ACCOUNT AS T2 ON T4.ACCOUNT_ID = T2.ACCOUNT_ID INNER JOIN TRANS AS T3 ON T2.ACCOUNT_ID = T3.ACCOUNT_ID WHERE STRFTIME('%Y', T3.DATE)= '1998' AND T1.CLIENT_ID = 617;
