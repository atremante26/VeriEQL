-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (0, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO CARD VALUES (0, 0, 'GOLD');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (0, 0, 0, 'DISPONENT');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (0, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO TRANS VALUES (0, 0, '2147483648');
-- ----------sql1------------
-- 1
SELECT COUNT(T1.CARD_ID) FROM CARD AS T1 INNER JOIN DISP AS T2 ON T1.DISP_ID = T2.DISP_ID WHERE T1.TYPE = 'GOLD' AND T2.TYPE = 'DISPONENT';
-- ----------sql2------------
-- 0
SELECT COUNT(T1.CARD_ID) FROM CARD AS T1 INNER JOIN DISP AS T2 ON T1.DISP_ID = T2.DISP_ID WHERE T1.TYPE = 'GOLD' AND T2.TYPE = 'OWNER';
