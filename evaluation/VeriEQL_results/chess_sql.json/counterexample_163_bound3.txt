-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (1, 0);
INSERT INTO ACCOUNT VALUES (0, 0);
INSERT INTO ACCOUNT VALUES (-1, 2);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, -1);
INSERT INTO CARD VALUES (1, -2);
INSERT INTO CARD VALUES (-1, -1);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (-1, 1);
INSERT INTO CLIENT VALUES (1, 0);
INSERT INTO CLIENT VALUES (0, 2);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 1);
INSERT INTO DISP VALUES (-1, 0, 1);
INSERT INTO DISP VALUES (-2, 1, -1);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A2 VARCHAR(20)
);
INSERT INTO DISTRICT VALUES (0, '2147483649');
INSERT INTO DISTRICT VALUES (1, '2147483648');
INSERT INTO DISTRICT VALUES (2, '2147483650');
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (0, -1, 'A');
INSERT INTO LOAN VALUES (-1, 1, 'A');
INSERT INTO LOAN VALUES (-2, 0, 'A');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (-1, 0);
INSERT INTO ORDER VALUES (0, 1);
INSERT INTO ORDER VALUES (1, 1);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, 0);
INSERT INTO TRANS VALUES (-2, 1, 0);
INSERT INTO TRANS VALUES (-1, 1, 0);
-- ----------sql1------------
-- 2147483649
SELECT T3.A2 FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID INNER JOIN DISTRICT AS T3 ON T2.DISTRICT_ID = T3.DISTRICT_ID WHERE T1.STATUS = 'A' GROUP BY T3.A2 ORDER BY COUNT(T1.ACCOUNT_ID) DESC LIMIT 1;
-- ----------sql2------------
-- 2147483650
SELECT T1.A2 FROM DISTRICT AS T1 INNER JOIN ACCOUNT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID INNER JOIN LOAN AS T3 ON T2.ACCOUNT_ID = T3.ACCOUNT_ID WHERE T3.STATUS = 'A' GROUP BY T1.DISTRICT_ID ORDER BY COUNT(T2.ACCOUNT_ID) DESC LIMIT 1;
