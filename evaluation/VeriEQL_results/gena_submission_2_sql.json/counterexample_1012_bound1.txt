-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CIRCUITS (
	CIRCUITID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO CIRCUITS VALUES (0, '2147483648');
CREATE TABLE CONSTRUCTORS (
	CONSTRUCTORID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO CONSTRUCTORS VALUES (0, '2147483648');
CREATE TABLE DRIVERS (
	DRIVERID INTEGER,
	FORENAME VARCHAR(20),
	SURNAME VARCHAR(20)
);
INSERT INTO DRIVERS VALUES (0, 'LEWIS', 'HAMILTON');
CREATE TABLE SEASONS (
	YEAR INTEGER
);
INSERT INTO SEASONS VALUES (0);
CREATE TABLE RACES (
	RACEID INTEGER,
	YEAR INTEGER,
	CIRCUITID INTEGER,
	NAME VARCHAR(20),
	TIME VARCHAR(20)
);
INSERT INTO RACES VALUES (0, 0, 0, '2147483648', '2147483648');
CREATE TABLE CONSTRUCTORRESULTS (
	CONSTRUCTORRESULTSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER
);
INSERT INTO CONSTRUCTORRESULTS VALUES (0, 0, 0);
CREATE TABLE CONSTRUCTORSTANDINGS (
	CONSTRUCTORSTANDINGSID INTEGER,
	RACEID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER
);
INSERT INTO CONSTRUCTORSTANDINGS VALUES (0, 0, 0, 0);
CREATE TABLE DRIVERSTANDINGS (
	DRIVERSTANDINGSID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	POSITION INTEGER
);
INSERT INTO DRIVERSTANDINGS VALUES (0, 0, 0, 0);
CREATE TABLE LAPTIMES (
	RACEID INTEGER,
	DRIVERID INTEGER,
	LAP INTEGER,
	POSITION INTEGER,
	TIME VARCHAR(20),
	MILLISECONDS INTEGER
);
INSERT INTO LAPTIMES VALUES (0, 0, 0, 0, '2147483648', 0);
CREATE TABLE PITSTOPS (
	RACEID INTEGER,
	DRIVERID INTEGER,
	STOP INTEGER,
	LAP INTEGER,
	TIME VARCHAR(20),
	MILLISECONDS INTEGER
);
INSERT INTO PITSTOPS VALUES (0, 0, 0, 0, '2147483648', 0);
CREATE TABLE QUALIFYING (
	QUALIFYID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER
);
INSERT INTO QUALIFYING VALUES (0, 0, 0, 0, 0);
CREATE TABLE STATUS (
	STATUSID INTEGER
);
INSERT INTO STATUS VALUES (0);
CREATE TABLE RESULTS (
	RESULTID INTEGER,
	RACEID INTEGER,
	DRIVERID INTEGER,
	CONSTRUCTORID INTEGER,
	POSITION INTEGER,
	TIME VARCHAR(20),
	MILLISECONDS INTEGER,
	STATUSID INTEGER
);
INSERT INTO RESULTS VALUES (0, 0, 0, 0, -1, '2147483648', 0, 0);
-- ----------sql1------------
-- -1
SELECT T1.POSITION FROM RESULTS AS T1 INNER JOIN DRIVERS AS T2 ON T1.DRIVERID = T2.DRIVERID WHERE T2.FORENAME = 'LEWIS' AND T2.SURNAME = 'HAMILTON' ORDER BY T1.MILLISECONDS ASC LIMIT 1;
-- ----------sql2------------
-- 0
SELECT T1.POSITION FROM LAPTIMES AS T1 INNER JOIN DRIVERS AS T2 ON T1.DRIVERID = T2.DRIVERID WHERE T2.FORENAME = 'LEWIS' AND T2.SURNAME = 'HAMILTON' ORDER BY T1.TIME ASC LIMIT 1;
