-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	DIAGNOSIS VARCHAR(20),
	SYMPTOMS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (0, '2147483648', '2147483648');
CREATE TABLE PATIENT (
	ID INTEGER,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (0, 'SLE');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	PT INTEGER
);
INSERT INTO LABORATORY VALUES (0, '1000-01-01', 0, 0);
-- ----------sql1------------
-- 2147483648
SELECT T1.SYMPTOMS FROM EXAMINATION AS T1 INNER JOIN PATIENT AS T2 ON T1.ID = T2.ID WHERE T2.DIAGNOSIS = 'SLE' GROUP BY T1.SYMPTOMS ORDER BY COUNT(T1.SYMPTOMS) DESC LIMIT 1;
-- ----------sql2------------
SELECT SYMPTOMS FROM EXAMINATION WHERE DIAGNOSIS = 'SLE' GROUP BY SYMPTOMS ORDER BY COUNT(SYMPTOMS) DESC LIMIT 1;
