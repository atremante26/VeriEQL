-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO ACCOUNT VALUES (1, -2);
INSERT INTO ACCOUNT VALUES (0, 0);
INSERT INTO ACCOUNT VALUES (2, 0);
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (1, -1);
INSERT INTO CARD VALUES (0, -1);
INSERT INTO CARD VALUES (-1, -2);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (1, 0);
INSERT INTO CLIENT VALUES (2, -1);
INSERT INTO CLIENT VALUES (0, -1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 2);
INSERT INTO DISP VALUES (-2, 2, 2);
INSERT INTO DISP VALUES (-1, 1, 2);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A16 INTEGER
);
INSERT INTO DISTRICT VALUES (-1, 3);
INSERT INTO DISTRICT VALUES (-2, 1);
INSERT INTO DISTRICT VALUES (0, 2);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO LOAN VALUES (2, 0);
INSERT INTO LOAN VALUES (1, 2);
INSERT INTO LOAN VALUES (0, 0);
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO ORDER VALUES (-2, 1);
INSERT INTO ORDER VALUES (-1, 0);
INSERT INTO ORDER VALUES (0, 1);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (-2, 2, 0);
INSERT INTO TRANS VALUES (-1, 0, 0);
INSERT INTO TRANS VALUES (0, 0, 0);
-- ----------sql1------------
-- 0
SELECT COUNT(T1.ACCOUNT_ID) FROM ACCOUNT AS T1 INNER JOIN DISTRICT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID WHERE T2.A16 = ( SELECT MAX(A16) FROM DISTRICT );
-- ----------sql2------------
-- 2
SELECT COUNT(T2.ACCOUNT_ID) FROM DISTRICT AS T1 INNER JOIN ACCOUNT AS T2 ON T1.DISTRICT_ID = T2.DISTRICT_ID GROUP BY T1.A16 ORDER BY T1.A16 DESC LIMIT 1;
