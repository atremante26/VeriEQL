-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (0, NULL);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	RA VARCHAR(20),
	SSA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (0, '1000-01-01', '2147483648', '2147483648');
-- ----------sql1------------
-- 0
SELECT T1.ID FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.SSA NOT IN ('NEGATIVE', '0') ORDER BY T1.FIRST_DATE LIMIT 1;
-- ----------sql2------------
SELECT T1.ID FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T1.FIRST_DATE IS NOT NULL AND T2.SSA NOT IN ('NEGATIVE', '0') ORDER BY T1.FIRST_DATE ASC LIMIT 1;
