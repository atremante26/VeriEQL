-- ----------An counterexample found by VeriEQL------------
CREATE TABLE FRPM (
	CDSCODE VARCHAR(20)
);
INSERT INTO FRPM VALUES ('2147483648');
CREATE TABLE SATSCORES (
	CDS VARCHAR(20)
);
INSERT INTO SATSCORES VALUES ('2147483648');
CREATE TABLE SCHOOLS (
	CDSCODE VARCHAR(20),
	STATUSTYPE VARCHAR(20),
	COUNTY VARCHAR(20),
	SCHOOL VARCHAR(20),
	CLOSEDDATE DATE
);
INSERT INTO SCHOOLS VALUES ('2147483648', 'CLOSED', NULL, '2147483648', NULL);
-- ----------sql1------------
-- NULL, 2147483648, NULL
SELECT T1.COUNTY, T1.SCHOOL, T1.CLOSEDDATE FROM SCHOOLS AS T1 WHERE T1.STATUSTYPE = 'CLOSED' GROUP BY T1.COUNTY ORDER BY COUNT(T1.SCHOOL) DESC LIMIT 1;
-- ----------sql2------------
SELECT DISTINCT COUNTY, SCHOOL, CLOSEDDATE FROM SCHOOLS WHERE COUNTY = ( SELECT COUNTY FROM SCHOOLS WHERE STATUSTYPE = 'CLOSED' GROUP BY COUNTY ORDER BY COUNT(SCHOOL) DESC LIMIT 1 ) AND STATUSTYPE = 'CLOSED' AND SCHOOL IS NOT NULL;
