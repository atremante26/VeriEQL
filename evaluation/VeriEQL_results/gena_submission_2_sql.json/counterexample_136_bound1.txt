-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (0, 0, 'POPLATEK MESICNE', '1000-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER
);
INSERT INTO CARD VALUES (0, 0);
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (0, 0);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER
);
INSERT INTO DISP VALUES (0, 0, 0);
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER
);
INSERT INTO DISTRICT VALUES (0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (0, 0, '1995-01-01', 250000, '1481817355109550463');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	AMOUNT INTEGER
);
INSERT INTO ORDER VALUES (0, 0, 0);
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (0, 0, '1000-01-01', 0, 0);
-- ----------sql1------------
-- 0
SELECT COUNT(DISTINCT T1.ACCOUNT_ID) FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T1.AMOUNT >= 250000 AND T2.FREQUENCY = 'POPLATEK MESICNE' AND T1.DATE BETWEEN '1995-01-01' AND '1997-12-31' AND T1.STATUS = 'A';
-- ----------sql2------------
-- 1
SELECT COUNT(T1.ACCOUNT_ID) FROM ACCOUNT AS T1 INNER JOIN LOAN AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T2.DATE BETWEEN '1995-01-01' AND '1997-12-31' AND T1.FREQUENCY = 'POPLATEK MESICNE' AND T2.AMOUNT >= 250000;
