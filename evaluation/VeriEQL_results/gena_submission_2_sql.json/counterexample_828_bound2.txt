-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER
);
INSERT INTO ALIGNMENT VALUES (1);
INSERT INTO ALIGNMENT VALUES (0);
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (0, 'SPEED');
INSERT INTO ATTRIBUTE VALUES (1, 'SPEED');
CREATE TABLE COLOUR (
	ID INTEGER
);
INSERT INTO COLOUR VALUES (1);
INSERT INTO COLOUR VALUES (0);
CREATE TABLE GENDER (
	ID INTEGER
);
INSERT INTO GENDER VALUES (1);
INSERT INTO GENDER VALUES (0);
CREATE TABLE PUBLISHER (
	ID INTEGER
);
INSERT INTO PUBLISHER VALUES (0);
INSERT INTO PUBLISHER VALUES (-1);
CREATE TABLE RACE (
	ID INTEGER
);
INSERT INTO RACE VALUES (0);
INSERT INTO RACE VALUES (-1);
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER
);
INSERT INTO SUPERHERO VALUES (0, NULL, 1, 0, 1, 1, 0, -1, 1);
INSERT INTO SUPERHERO VALUES (-1, '2147483648', 1, 1, 1, 1, -1, 0, 1);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (0, 1, 100);
INSERT INTO HERO_ATTRIBUTE VALUES (0, 1, 100);
CREATE TABLE SUPERPOWER (
	ID INTEGER
);
INSERT INTO SUPERPOWER VALUES (-1);
INSERT INTO SUPERPOWER VALUES (0);
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (-1, 0);
INSERT INTO HERO_POWER VALUES (0, -1);
-- ----------sql1------------
-- 2
SELECT COUNT(T1.HERO_ID) FROM HERO_ATTRIBUTE AS T1 INNER JOIN ATTRIBUTE AS T2 ON T1.ATTRIBUTE_ID = T2.ID WHERE T2.ATTRIBUTE_NAME = 'SPEED' AND T1.ATTRIBUTE_VALUE = 100;
-- ----------sql2------------
-- 0
SELECT COUNT(T3.SUPERHERO_NAME) FROM HERO_ATTRIBUTE AS T1 INNER JOIN ATTRIBUTE AS T2 ON T1.ATTRIBUTE_ID = T2.ID INNER JOIN SUPERHERO AS T3 ON T1.HERO_ID = T3.ID WHERE T2.ATTRIBUTE_NAME = 'SPEED' AND T1.ATTRIBUTE_VALUE = 100;
