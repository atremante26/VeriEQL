-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER
);
INSERT INTO EXAMINATION VALUES (0);
INSERT INTO EXAMINATION VALUES (0);
CREATE TABLE PATIENT (
	ID INTEGER,
	FIRST_DATE DATE
);
INSERT INTO PATIENT VALUES (0, '1000-01-02');
INSERT INTO PATIENT VALUES (-1, '1000-01-01');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	LDH INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '1000-01-02', 500, '2147483648');
INSERT INTO LABORATORY VALUES (0, '1000-01-03', 500, '2147483648');
-- ----------sql1------------
-- 3
SELECT MAX(T1.DATE) FROM PATIENT AS T2 INNER JOIN LABORATORY AS T1 ON T2.ID = T1.ID WHERE T1.LDH >= 500;
-- ----------sql2------------
-- 2
SELECT T1.FIRST_DATE FROM PATIENT AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.LDH >= 500 ORDER BY T1.FIRST_DATE DESC LIMIT 1;
