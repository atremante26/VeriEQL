-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20),
	DATE DATE
);
INSERT INTO BADGES VALUES (1, -1, 'DZc', '2014-12-01');
INSERT INTO BADGES VALUES (0, 0, '', '2013-01-01');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER,
	USERDISPLAYNAME VARCHAR(20)
);
INSERT INTO COMMENTS VALUES (-1, 1, '1000-12-01', -1, '');
INSERT INTO COMMENTS VALUES (0, 0, '1000-05-01', -1, 'D');
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER,
	USERDISPLAYNAME VARCHAR(20)
);
INSERT INTO POSTHISTORY VALUES (1, 0, '2014-08-01', 0, 'JB');
INSERT INTO POSTHISTORY VALUES (0, 1, '2014-12-01', -1, 'JJ');
CREATE TABLE POSTLINKS (
	ID INTEGER,
	CREATIONDATE DATE,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, '1000-06-01', 0, 1);
INSERT INTO POSTLINKS VALUES (-1, '1000-03-01', 1, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	CREAIONDATE DATE,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, '2013-01-01', 0, -1, 0);
INSERT INTO POSTS VALUES (1, '2012-12-01', 0, -1, 1);
CREATE TABLE TAGS (
	ID INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0);
INSERT INTO TAGS VALUES (0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	CREATIONDATE DATE,
	DISPLAYNAME VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, NULL, '');
INSERT INTO USERS VALUES (0, NULL, 'SAMUEL');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	CREATIONDATE DATE,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-1, 0, '1000-12-01', 0);
INSERT INTO VOTES VALUES (0, 0, '1000-12-01', 0);
-- ----------sql1------------
-- 0, 
SELECT T1.ID, T2.NAME FROM POSTS AS T1 INNER JOIN BADGES AS T2 ON T1.OWNERUSERID = T2.USERID INNER JOIN USERS AS T3 ON T1.OWNERUSERID = T3.ID WHERE T3.DISPLAYNAME = 'SAMUEL' AND STRFTIME('%Y', T1.CREAIONDATE) = '2013' AND STRFTIME('%Y', T2.DATE) = '2013';
-- ----------sql2------------
SELECT T1.POSTID, T2.NAME FROM POSTHISTORY AS T1 INNER JOIN BADGES AS T2 ON T1.USERID = T2.USERID WHERE T1.USERDISPLAYNAME = 'SAMUEL' AND STRFTIME('%Y', T1.CREATIONDATE) = '2013' AND STRFTIME('%Y', T2.DATE) = '2013';
