-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (-2, 0, '2147483648');
INSERT INTO BADGES VALUES (0, -1, 'TEACHER');
INSERT INTO BADGES VALUES (-1, -1, 'SUPPORTER');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, -1, 0);
INSERT INTO COMMENTS VALUES (1, 0, -2);
INSERT INTO COMMENTS VALUES (2, -1, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-2, 0, -1);
INSERT INTO POSTHISTORY VALUES (0, 0, -2);
INSERT INTO POSTHISTORY VALUES (-1, 0, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (1, 1, -1);
INSERT INTO POSTLINKS VALUES (0, -1, 0);
INSERT INTO POSTLINKS VALUES (-1, 1, -1);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-1, 0, -1, -1);
INSERT INTO POSTS VALUES (1, -1, -1, 1);
INSERT INTO POSTS VALUES (0, 0, -2, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (1, 0, 0);
INSERT INTO TAGS VALUES (0, 0, -1);
INSERT INTO TAGS VALUES (2, 0, -1);
CREATE TABLE USERS (
	ID INTEGER,
	LOCATION VARCHAR(20)
);
INSERT INTO USERS VALUES (-1, 'NEW YORK');
INSERT INTO USERS VALUES (-2, '2147483648');
INSERT INTO USERS VALUES (0, '2147483648');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 1, 0);
INSERT INTO VOTES VALUES (-1, 1, -1);
INSERT INTO VOTES VALUES (0, -1, -2);
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT T1.USERID) FROM BADGES AS T1 INNER JOIN BADGES AS T2 ON T1.USERID = T2.USERID INNER JOIN USERS AS T3 ON T1.USERID = T3.ID WHERE T3.LOCATION = 'NEW YORK' AND T1.NAME = 'TEACHER' AND T2.NAME = 'SUPPORTER';
-- ----------sql2------------
-- 2
SELECT COUNT(DISTINCT T1.ID) FROM BADGES AS T1 INNER JOIN USERS AS T2 ON T1.USERID = T2.ID WHERE T1.NAME IN ('SUPPORTER', 'TEACHER') AND T2.LOCATION = 'NEW YORK';
