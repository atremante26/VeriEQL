-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (0, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	TEXT VARCHAR(20),
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, '2147483649', 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER,
	TEXT VARCHAR(20),
	COMMENT VARCHAR(20)
);
INSERT INTO POSTHISTORY VALUES (0, 0, 0, '2147483648', '2147483648');
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	TITLE VARCHAR(20),
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, 'WHY SQUARE THE DIFFERENCE INSTEAD OF TAKING THE ABSOLUTE VALUE IN STANDARD DEVIATION?', 0, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (0, 0);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, 0, 0);
-- ----------sql1------------
-- 2147483649
SELECT COMMENTS.TEXT FROM COMMENTS INNER JOIN POSTHISTORY ON COMMENTS.USERID = POSTHISTORY.USERID INNER JOIN POSTS ON POSTHISTORY.POSTID = POSTS.ID WHERE POSTS.TITLE = 'WHY SQUARE THE DIFFERENCE INSTEAD OF TAKING THE ABSOLUTE VALUE IN STANDARD DEVIATION?';
-- ----------sql2------------
-- 2147483648
SELECT T2.COMMENT FROM POSTS AS T1 INNER JOIN POSTHISTORY AS T2 ON T1.ID = T2.POSTID WHERE T1.TITLE = 'WHY SQUARE THE DIFFERENCE INSTEAD OF TAKING THE ABSOLUTE VALUE IN STANDARD DEVIATION?';
