-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (0, -1);
INSERT INTO BADGES VALUES (1, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, -10, -1);
INSERT INTO COMMENTS VALUES (1, -10, -1);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, -10, 0);
INSERT INTO POSTHISTORY VALUES (0, -11, -1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (0, -10, -11);
INSERT INTO POSTLINKS VALUES (1, -10, -11);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-10, -1, -1, -11);
INSERT INTO POSTS VALUES (-11, -1, 0, -11);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, -10);
INSERT INTO TAGS VALUES (0, 0, -10);
CREATE TABLE USERS (
	ID INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (-1, 1);
INSERT INTO USERS VALUES (0, NULL);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-1, -10, 0);
INSERT INTO VOTES VALUES (0, -10, -1);
-- ----------sql1------------
-- 1
SELECT AVG(VOTE_COUNT) FROM (SELECT COUNT(T2.POSTID) AS VOTE_COUNT FROM USERS AS T1 INNER JOIN VOTES AS T2 ON T1.ID = T2.USERID WHERE T1.AGE = (SELECT MAX(AGE) FROM USERS) GROUP BY T1.ID) AS VOTE_COUNTS;
-- ----------sql2------------
-- -10
SELECT AVG(POSTID) FROM VOTES WHERE USERID IN ( SELECT ID FROM USERS WHERE AGE = ( SELECT MAX(AGE) FROM USERS ) );
