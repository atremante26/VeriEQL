-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER,
	NAME VARCHAR(20)
);
INSERT INTO BADGES VALUES (-2, 0, 'TEACHER');
INSERT INTO BADGES VALUES (0, 3, '893372325466041125');
INSERT INTO BADGES VALUES (-1, 0, '893372325466041127');
INSERT INTO BADGES VALUES (1, 3, 'TEACHER');
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (-3, 3, 0);
INSERT INTO COMMENTS VALUES (-2, 2, 0);
INSERT INTO COMMENTS VALUES (-1, 3, 0);
INSERT INTO COMMENTS VALUES (0, 3, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-1, 3, 0);
INSERT INTO POSTHISTORY VALUES (0, 0, 0);
INSERT INTO POSTHISTORY VALUES (1, 1, 0);
INSERT INTO POSTHISTORY VALUES (2, 3, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-3, 3, 1);
INSERT INTO POSTLINKS VALUES (-2, 3, 3);
INSERT INTO POSTLINKS VALUES (-1, 3, 0);
INSERT INTO POSTLINKS VALUES (0, 3, 3);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (2, 0, 0, 3);
INSERT INTO POSTS VALUES (1, 0, 0, 3);
INSERT INTO POSTS VALUES (0, 0, 0, 3);
INSERT INTO POSTS VALUES (3, 0, 0, 3);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-3, 0, 0);
INSERT INTO TAGS VALUES (-2, 0, 3);
INSERT INTO TAGS VALUES (-1, 0, 3);
INSERT INTO TAGS VALUES (0, 0, 3);
CREATE TABLE USERS (
	ID INTEGER
);
INSERT INTO USERS VALUES (3);
INSERT INTO USERS VALUES (2);
INSERT INTO USERS VALUES (1);
INSERT INTO USERS VALUES (0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 3, 0);
INSERT INTO VOTES VALUES (-1, 3, 0);
INSERT INTO VOTES VALUES (1, 3, 0);
INSERT INTO VOTES VALUES (0, 3, 0);
-- ----------sql1------------
-- 100
SELECT CAST(COUNT(DISTINCT CASE WHEN BADGES.NAME = 'TEACHER' THEN BADGES.USERID ELSE NULL END) AS REAL) * 100 / COUNT(DISTINCT USERS.ID) FROM USERS INNER JOIN BADGES ON USERS.ID = BADGES.USERID;
-- ----------sql2------------
-- 50
SELECT CAST(COUNT(T1.ID) AS REAL) * 100 / (SELECT COUNT(ID) FROM USERS) FROM USERS AS T1 INNER JOIN BADGES AS T2 ON T1.ID = T2.USERID WHERE T2.NAME = 'TEACHER';
