-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ALIGNMENT (
	ID INTEGER,
	ALIGNMENT VARCHAR(20)
);
INSERT INTO ALIGNMENT VALUES (2, 'Good');
INSERT INTO ALIGNMENT VALUES (1, 'Neutral');
CREATE TABLE ATTRIBUTE (
	ID INTEGER,
	ATTRIBUTE_NAME VARCHAR(20)
);
INSERT INTO ATTRIBUTE VALUES (2, 'Strength');
INSERT INTO ATTRIBUTE VALUES (1, 'Strength');
CREATE TABLE COLOUR (
	ID INTEGER,
	COLOUR VARCHAR(20)
);
INSERT INTO COLOUR VALUES (13, '2147483648');
INSERT INTO COLOUR VALUES (1, '2147483648');
CREATE TABLE GENDER (
	ID INTEGER,
	GENDER VARCHAR(20)
);
INSERT INTO GENDER VALUES (1, 'Male');
INSERT INTO GENDER VALUES (3, 'Male');
CREATE TABLE PUBLISHER (
	ID INTEGER,
	PUBLISHER_NAME VARCHAR(20)
);
INSERT INTO PUBLISHER VALUES (1, 'J. K. Rowling');
INSERT INTO PUBLISHER VALUES (17, 'Icon Comics');
CREATE TABLE RACE (
	ID INTEGER,
	RACE VARCHAR(20)
);
INSERT INTO RACE VALUES (1, '2147483648');
INSERT INTO RACE VALUES (2, '2147483648');
CREATE TABLE SUPERHERO (
	ID INTEGER,
	SUPERHERO_NAME VARCHAR(20),
	FULL_NAME VARCHAR(20),
	GENDER_ID INTEGER,
	EYE_COLOUR_ID INTEGER,
	HAIR_COLOUR_ID INTEGER,
	SKIN_COLOUR_ID INTEGER,
	RACE_ID INTEGER,
	PUBLISHER_ID INTEGER,
	ALIGNMENT_ID INTEGER,
	HEIGHT_CM INTEGER,
	WEIGHT_KG INTEGER
);
INSERT INTO SUPERHERO VALUES (1, '2147483648', '2147483648', 1, 13, 1, 13, 1, 1, 2, 0, 0);
INSERT INTO SUPERHERO VALUES (2, '2147483649', '2147483648', 3, 13, 1, 1, 2, 1, 1, 0, 0);
CREATE TABLE HERO_ATTRIBUTE (
	HERO_ID INTEGER,
	ATTRIBUTE_ID INTEGER,
	ATTRIBUTE_VALUE INTEGER
);
INSERT INTO HERO_ATTRIBUTE VALUES (2, 2, 10);
INSERT INTO HERO_ATTRIBUTE VALUES (1, 2, 10);
CREATE TABLE SUPERPOWER (
	ID INTEGER,
	POWER_NAME VARCHAR(20)
);
INSERT INTO SUPERPOWER VALUES (2, '2147483648');
INSERT INTO SUPERPOWER VALUES (1, '2147483648');
CREATE TABLE HERO_POWER (
	HERO_ID INTEGER,
	POWER_ID INTEGER
);
INSERT INTO HERO_POWER VALUES (2, 2);
INSERT INTO HERO_POWER VALUES (2, 2);
-- ----------sql1------------
-- 0
SELECT CAST( COUNT(CASE WHEN GENDER_ID = 2 THEN 1 ELSE NULL END) AS REAL ) * 100.0 / COUNT(*) AS PERCENTAGE_FEMALE FROM SUPERHERO WHERE ALIGNMENT_ID = 2;
-- ----------sql2------------
-- NULL
SELECT CAST(COUNT(CASE WHEN T3.GENDER = 'FEMALE' THEN T1.ID ELSE NULL END) AS REAL) * 100 / COUNT(T1.ID) FROM SUPERHERO AS T1 INNER JOIN ALIGNMENT AS T2 ON T1.ALIGNMENT_ID = T2.ID INNER JOIN GENDER AS T3 ON T1.GENDER_ID = T3.ID WHERE T2.ALIGNMENT = 'BAD';
