-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (1, 1, 'X', '4000-12-01');
INSERT INTO ACCOUNT VALUES (0, 0, 'Y', '1000-10-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20),
	ISSUED DATE
);
INSERT INTO CARD VALUES (0, 0, 'Z', '1000-02-01');
INSERT INTO CARD VALUES (1, 0, '', '1000-12-01');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	GENDER VARCHAR(20),
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (1, '[', '1000-08-01', 0);
INSERT INTO CLIENT VALUES (0, '', '1000-01-01', 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (0, 0, 0, '');
INSERT INTO DISP VALUES (1, 0, 0, '');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A2 VARCHAR(20),
	A3 VARCHAR(20),
	A4 VARCHAR(20),
	A5 VARCHAR(20),
	A6 VARCHAR(20),
	A7 VARCHAR(20),
	A8 INTEGER,
	A9 INTEGER,
	A10 INTEGER,
	A11 INTEGER,
	A12 INTEGER,
	A13 INTEGER,
	A14 INTEGER,
	A15 INTEGER,
	A16 INTEGER
);
INSERT INTO DISTRICT VALUES (1, ']', '^', '_', '`', '', '', 0, 0, 0, 0, 0, 0, 0, 0, 0);
INSERT INTO DISTRICT VALUES (0, 'a', 'b', '', 'c', '', '', 0, 0, 0, 0, 0, 0, 0, 0, 0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER,
	PAYMENTS INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (1, 0, '1000-06-01', -11, 25, 55, 'VW');
INSERT INTO LOAN VALUES (0, 1, '1000-01-01', -11, 25, 56, '');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	BANK_TO VARCHAR(20),
	ACCOUNT_TO INTEGER,
	AMOUNT INTEGER,
	K_SYMBOL VARCHAR(20)
);
INSERT INTO ORDER VALUES (0, 0, '', 0, 0, '');
INSERT INTO ORDER VALUES (-1, 0, '', 0, 0, '');
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	TYPE VARCHAR(20),
	OPERATION VARCHAR(20),
	AMOUNT INTEGER,
	BALANCE INTEGER,
	K_SYMBOL VARCHAR(20),
	BANK VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (1, 0, '1000-05-01', '', '', 0, 0, 'd', 'e', 0);
INSERT INTO TRANS VALUES (0, 0, '1000-03-01', '', 'f', 0, 0, 'g', 'h', 0);
-- ----------sql1------------
SELECT A.ACCOUNT_ID FROM LOAN L INNER JOIN ACCOUNT A ON L.ACCOUNT_ID = A.ACCOUNT_ID WHERE L.DURATION > 24 AND L.STATUS = 'A' AND A.DATE < '1997-01-01' AND L.AMOUNT = ( SELECT MIN(AMOUNT) FROM LOAN WHERE DURATION > 24 AND STATUS = 'A' );
-- ----------sql2------------
-- 0
SELECT T1.ACCOUNT_ID FROM LOAN AS T1 INNER JOIN ACCOUNT AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID WHERE T1.DURATION > 24 AND STRFTIME('%Y', T2.DATE) < '1997' ORDER BY T1.AMOUNT ASC LIMIT 1;
