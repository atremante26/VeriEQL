-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ACCOUNT (
	ACCOUNT_ID INTEGER,
	DISTRICT_ID INTEGER,
	FREQUENCY VARCHAR(20),
	DATE DATE
);
INSERT INTO ACCOUNT VALUES (1, 1, 'POPLATEK TYDNE', '0001-01-01');
INSERT INTO ACCOUNT VALUES (2, 2, 'POPLATEK MESICNE', '0001-01-01');
CREATE TABLE CARD (
	CARD_ID INTEGER,
	DISP_ID INTEGER,
	TYPE VARCHAR(20),
	ISSUED DATE
);
INSERT INTO CARD VALUES (0, 1, 'classic', '0001-01-01');
INSERT INTO CARD VALUES (1, 2, 'classic', '0001-01-01');
CREATE TABLE CLIENT (
	CLIENT_ID INTEGER,
	GENDER VARCHAR(20),
	BIRTH_DATE DATE,
	DISTRICT_ID INTEGER
);
INSERT INTO CLIENT VALUES (3, 'F', '0001-01-01', 2);
INSERT INTO CLIENT VALUES (2, 'M', '0001-01-02', 1);
CREATE TABLE DISP (
	DISP_ID INTEGER,
	CLIENT_ID INTEGER,
	ACCOUNT_ID INTEGER,
	TYPE VARCHAR(20)
);
INSERT INTO DISP VALUES (2, 2, 1, 'OWNER');
INSERT INTO DISP VALUES (1, 2, 2, 'OWNER');
CREATE TABLE DISTRICT (
	DISTRICT_ID INTEGER,
	A2 VARCHAR(20),
	A3 VARCHAR(20),
	A4 VARCHAR(20),
	A5 VARCHAR(20),
	A6 VARCHAR(20),
	A7 VARCHAR(20),
	A8 INTEGER,
	A9 INTEGER,
	A10 INTEGER,
	A11 INTEGER,
	A12 INTEGER,
	A13 INTEGER,
	A14 INTEGER,
	A15 INTEGER,
	A16 INTEGER
);
INSERT INTO DISTRICT VALUES (1, '2147483648', 'south Bohemia', '2147483648', '2147483648', '2147483648', NULL, 4, 0, 0, 9922, 0, 0, 0, 0, 0);
INSERT INTO DISTRICT VALUES (2, '2147483649', 'central Bohemia', '2147483648', NULL, NULL, NULL, 2, 0, 0, 9921, 0, 0, 0, 0, 0);
CREATE TABLE LOAN (
	LOAN_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	AMOUNT INTEGER,
	DURATION INTEGER,
	PAYMENTS INTEGER,
	STATUS VARCHAR(20)
);
INSERT INTO LOAN VALUES (1, 2, '0001-01-01', 0, 0, 0, 'C');
INSERT INTO LOAN VALUES (2, 2, '0001-01-01', 0, 0, 0, 'A');
CREATE TABLE ORDER (
	ORDER_ID INTEGER,
	ACCOUNT_ID INTEGER,
	BANK_TO VARCHAR(20),
	ACCOUNT_TO INTEGER,
	AMOUNT INTEGER,
	K_SYMBOL VARCHAR(20)
);
INSERT INTO ORDER VALUES (2, 1, '2147483648', 0, 0, 'LEASING');
INSERT INTO ORDER VALUES (1, 2, '2147483648', 0, 0, 'POJISTNE');
CREATE TABLE TRANS (
	TRANS_ID INTEGER,
	ACCOUNT_ID INTEGER,
	DATE DATE,
	TYPE VARCHAR(20),
	OPERATION VARCHAR(20),
	AMOUNT INTEGER,
	BALANCE INTEGER,
	K_SYMBOL VARCHAR(20),
	BANK VARCHAR(20),
	ACCOUNT INTEGER
);
INSERT INTO TRANS VALUES (1, 2, '0001-01-01', 'VYDAJ', 'PREVOD Z UCTU', 0, 0, 'SLUZBY', '2147483648', 0);
INSERT INTO TRANS VALUES (2, 1, '0001-01-01', 'VYDAJ', 'VYBER KARTOU', 0, 0, 'UROK', '2147483648', 0);
-- ----------sql1------------
-- 2
SELECT A.ACCOUNT_ID FROM ACCOUNT A JOIN CLIENT C ON A.ACCOUNT_ID = C.CLIENT_ID JOIN DISTRICT D ON C.DISTRICT_ID = D.DISTRICT_ID WHERE D.A11 = (SELECT MAX(A11) FROM DISTRICT) ORDER BY C.BIRTH_DATE DESC LIMIT 1;
-- ----------sql2------------
-- 1
-- 2
SELECT T1.ACCOUNT_ID  FROM ACCOUNT AS T1 INNER JOIN DISP AS T2 ON T1.ACCOUNT_ID = T2.ACCOUNT_ID INNER JOIN CLIENT AS T3 ON T2.CLIENT_ID = T3.CLIENT_ID INNER JOIN DISTRICT AS T4 ON T4.DISTRICT_ID = T1.DISTRICT_ID WHERE T2.CLIENT_ID = ( SELECT CLIENT_ID FROM CLIENT ORDER BY BIRTH_DATE DESC LIMIT 1) GROUP BY T4.A11, T1.ACCOUNT_ID;
