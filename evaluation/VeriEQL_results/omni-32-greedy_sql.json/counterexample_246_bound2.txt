-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ATOM (
	ATOM_ID VARCHAR(20),
	MOLECULE_ID VARCHAR(20),
	ELEMENT VARCHAR(20)
);
INSERT INTO ATOM VALUES ('F', 'W', '');
INSERT INTO ATOM VALUES ('S\45', '', '');
CREATE TABLE BOND (
	BOND_ID VARCHAR(20),
	MOLECULE_ID VARCHAR(20),
	BOND_TYPE VARCHAR(20)
);
INSERT INTO BOND VALUES ('', '', '');
INSERT INTO BOND VALUES ('R', '', '');
CREATE TABLE CONNECTED (
	ATOM_ID VARCHAR(20),
	ATOM_ID2 VARCHAR(20),
	BOND_ID VARCHAR(20)
);
INSERT INTO CONNECTED VALUES ('F', 'S\45', '');
INSERT INTO CONNECTED VALUES ('S\45', 'F', '');
CREATE TABLE MOLECULE (
	MOLECULE_ID VARCHAR(20),
	LABEL VARCHAR(20)
);
INSERT INTO MOLECULE VALUES ('W', '');
INSERT INTO MOLECULE VALUES ('', '');
-- ----------sql1------------
-- , 
SELECT B.BOND_TYPE, B.BOND_ID FROM ATOM AS A INNER JOIN CONNECTED AS C ON A.ATOM_ID = C.ATOM_ID INNER JOIN BOND AS B ON C.BOND_ID = B.BOND_ID WHERE SUBSTR(A.ATOM_ID, -2) = '45';
-- ----------sql2------------
SELECT T1.BOND_TYPE, T1.BOND_ID FROM BOND AS T1 INNER JOIN CONNECTED AS T2 ON T1.BOND_ID = T2.BOND_ID WHERE SUBSTR(T2.ATOM_ID, 7, 2) = '45';
