-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (0, 'APS');
INSERT INTO EXAMINATION VALUES (0, 'APS');
CREATE TABLE PATIENT (
	ID INTEGER,
	SEX VARCHAR(20),
	DIAGNOSIS VARCHAR(20)
);
INSERT INTO PATIENT VALUES (0, 'F', '2147483648');
INSERT INTO PATIENT VALUES (1, '2147483648', '2147483648');
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER
);
INSERT INTO LABORATORY VALUES (0, '1000-01-01', 0);
INSERT INTO LABORATORY VALUES (1, '1000-01-02', 0);
-- ----------sql1------------
-- 2
SELECT COUNT(T1.ID) FROM PATIENT AS T1 INNER JOIN EXAMINATION AS T2 ON T1.ID = T2.ID WHERE T1.SEX = 'F' AND T2.DIAGNOSIS = 'APS';
-- ----------sql2------------
-- 0
SELECT COUNT(ID) FROM PATIENT WHERE SEX = 'F' AND DIAGNOSIS = 'APS';
