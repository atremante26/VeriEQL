-- ----------An counterexample found by VeriEQL------------
CREATE TABLE CUSTOMERS (
	CUSTOMERID INTEGER,
	SEGMENT VARCHAR(20),
	CURRENCY VARCHAR(20)
);
INSERT INTO CUSTOMERS VALUES (0, 'SME', '820075169991257113');
CREATE TABLE GASSTATIONS (
	GASSTATIONID INTEGER,
	SEGMENT VARCHAR(20)
);
INSERT INTO GASSTATIONS VALUES (0, '2147483648');
CREATE TABLE PRODUCTS (
	PRODUCTID INTEGER
);
INSERT INTO PRODUCTS VALUES (0);
CREATE TABLE TRANSACTIONS_1K (
	TRANSACTIONID INTEGER,
	DATE DATE,
	CUSTOMERID INTEGER,
	GASSTATIONID INTEGER,
	PRODUCTID INTEGER
);
INSERT INTO TRANSACTIONS_1K VALUES (0, '1000-01-01', 1, 0, 0);
CREATE TABLE YEARMONTH (
	CUSTOMERID INTEGER,
	DATE VARCHAR(20)
);
INSERT INTO YEARMONTH VALUES (0, '2147483648');
-- ----------sql1------------
-- NULL
SELECT SUM(CASE WHEN T1.CURRENCY = 'CZK' THEN 1 ELSE 0 END) - SUM(CASE WHEN T1.CURRENCY = 'EUR' THEN 1 ELSE 0 END) FROM CUSTOMERS AS T1 INNER JOIN TRANSACTIONS_1K AS T2 ON T1.CUSTOMERID = T2.CUSTOMERID WHERE T1.SEGMENT = 'SME';
-- ----------sql2------------
-- 0
SELECT SUM(CURRENCY = 'CZK') - SUM(CURRENCY = 'EUR') FROM CUSTOMERS WHERE SEGMENT = 'SME';
