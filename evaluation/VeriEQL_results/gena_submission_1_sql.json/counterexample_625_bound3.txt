-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-2, 0);
INSERT INTO BADGES VALUES (-1, 2);
INSERT INTO BADGES VALUES (0, 2);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, 2);
INSERT INTO COMMENTS VALUES (-2, -2, 2);
INSERT INTO COMMENTS VALUES (-1, 0, 2);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (-2, 0, 2);
INSERT INTO POSTHISTORY VALUES (-1, 0, 2);
INSERT INTO POSTHISTORY VALUES (0, -1, 0);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (2, 0, 0);
INSERT INTO POSTLINKS VALUES (0, 0, 0);
INSERT INTO POSTLINKS VALUES (1, 0, 0);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (-2, 2, 2, 0);
INSERT INTO POSTS VALUES (-1, 2, 2, 0);
INSERT INTO POSTS VALUES (0, 2, 2, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, 0);
INSERT INTO TAGS VALUES (0, 0, 0);
INSERT INTO TAGS VALUES (1, 0, 0);
CREATE TABLE USERS (
	ID INTEGER,
	LOCATION VARCHAR(20)
);
INSERT INTO USERS VALUES (0, 'NEW YORK, NY');
INSERT INTO USERS VALUES (1, 'NEW YORK, NY');
INSERT INTO USERS VALUES (2, 'NEW YORK, NY');
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (-2, 0, 2);
INSERT INTO VOTES VALUES (0, 0, 2);
INSERT INTO VOTES VALUES (-1, 0, 2);
-- ----------sql1------------
-- 3
SELECT COUNT(*) FROM USERS WHERE LOCATION = 'NEW YORK, NY';
-- ----------sql2------------
-- 0
SELECT COUNT(ID) FROM USERS WHERE LOCATION = 'NEW YORK';
