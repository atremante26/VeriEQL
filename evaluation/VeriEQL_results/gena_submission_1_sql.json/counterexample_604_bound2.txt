-- ----------An counterexample found by VeriEQL------------
CREATE TABLE BADGES (
	ID INTEGER,
	USERID INTEGER
);
INSERT INTO BADGES VALUES (-1, 1);
INSERT INTO BADGES VALUES (0, 0);
CREATE TABLE COMMENTS (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO COMMENTS VALUES (0, 0, 0);
INSERT INTO COMMENTS VALUES (1, -1, 0);
CREATE TABLE POSTHISTORY (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO POSTHISTORY VALUES (0, 0, 0);
INSERT INTO POSTHISTORY VALUES (1, -1, 1);
CREATE TABLE POSTLINKS (
	ID INTEGER,
	POSTID INTEGER,
	RELATEDPOSTID INTEGER
);
INSERT INTO POSTLINKS VALUES (-1, -1, -1);
INSERT INTO POSTLINKS VALUES (0, -1, -1);
CREATE TABLE POSTS (
	ID INTEGER,
	OWNERUSERID INTEGER,
	LASTEDITORUSERID INTEGER,
	PARENTID INTEGER
);
INSERT INTO POSTS VALUES (0, 0, 1, -1);
INSERT INTO POSTS VALUES (-1, 1, 1, 0);
CREATE TABLE TAGS (
	ID INTEGER,
	COUNT INTEGER,
	EXCERPTPOSTID INTEGER
);
INSERT INTO TAGS VALUES (-1, 0, -1);
INSERT INTO TAGS VALUES (0, 0, -1);
CREATE TABLE USERS (
	ID INTEGER,
	UPVOTES INTEGER,
	AGE INTEGER
);
INSERT INTO USERS VALUES (1, 0, 0);
INSERT INTO USERS VALUES (0, 0, 0);
CREATE TABLE VOTES (
	ID INTEGER,
	POSTID INTEGER,
	USERID INTEGER
);
INSERT INTO VOTES VALUES (0, -1, 1);
INSERT INTO VOTES VALUES (1, 0, 1);
-- ----------sql1------------
SELECT CAST(SUM(T1.UPVOTES) AS REAL) / COUNT(T1.ID), CAST(SUM(T1.AGE) AS REAL) / COUNT(T1.ID) FROM USERS AS T1 INNER JOIN POSTS AS T2 ON T1.ID = T2.OWNERUSERID GROUP BY T1.ID HAVING COUNT(T2.OWNERUSERID) > 10;
-- ----------sql2------------
-- NULL, NULL
SELECT AVG(T1.UPVOTES), AVG(T1.AGE) FROM USERS AS T1 INNER JOIN ( SELECT OWNERUSERID, COUNT(*) AS POST_COUNT FROM POSTS GROUP BY OWNERUSERID HAVING POST_COUNT > 10) AS T2 ON T1.ID = T2.OWNERUSERID;
