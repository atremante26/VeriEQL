-- ----------An counterexample found by VeriEQL------------
CREATE TABLE EXAMINATION (
	ID INTEGER,
	SYMPTOMS VARCHAR(20)
);
INSERT INTO EXAMINATION VALUES (0, '2147483648');
INSERT INTO EXAMINATION VALUES (0, '2147483648');
CREATE TABLE PATIENT (
	ID INTEGER
);
INSERT INTO PATIENT VALUES (-1);
INSERT INTO PATIENT VALUES (0);
CREATE TABLE LABORATORY (
	ID INTEGER,
	DATE DATE,
	UN INTEGER,
	TG INTEGER,
	PT INTEGER,
	RA VARCHAR(20)
);
INSERT INTO LABORATORY VALUES (-1, '1000-01-02', 0, 199, 12, '2147483648');
INSERT INTO LABORATORY VALUES (0, '1000-01-01', 0, 199, 13, '2147483648');
-- ----------sql1------------
-- 1
SELECT COUNT(DISTINCT T1.ID) FROM LABORATORY AS T1 INNER JOIN EXAMINATION AS T2 ON T1.ID = T2.ID WHERE T1.TG < 200 AND T2.SYMPTOMS IS NOT NULL;
-- ----------sql2------------
-- 2
SELECT COUNT(T1.ID) FROM EXAMINATION AS T1 INNER JOIN LABORATORY AS T2 ON T1.ID = T2.ID WHERE T2.TG < 200 AND T1.SYMPTOMS IS NOT NULL;
