-- ----------An counterexample found by VeriEQL------------
CREATE TABLE ATOM (
	ATOM_ID VARCHAR(20),
	MOLECULE_ID VARCHAR(20)
);
INSERT INTO ATOM VALUES ('RSTUC20', '');
INSERT INTO ATOM VALUES ('MNOPQ23', 'krs');
CREATE TABLE BOND (
	BOND_ID VARCHAR(20),
	MOLECULE_ID VARCHAR(20)
);
INSERT INTO BOND VALUES ('', '');
INSERT INTO BOND VALUES ('^', 'krs');
CREATE TABLE CONNECTED (
	ATOM_ID VARCHAR(20),
	ATOM_ID2 VARCHAR(20),
	BOND_ID VARCHAR(20)
);
INSERT INTO CONNECTED VALUES ('RSTUC20', 'MNOPQ23', '');
INSERT INTO CONNECTED VALUES ('MNOPQ23', 'RSTUC20', '^');
CREATE TABLE MOLECULE (
	MOLECULE_ID VARCHAR(20),
	LABEL VARCHAR(20)
);
INSERT INTO MOLECULE VALUES ('krs', '+');
INSERT INTO MOLECULE VALUES ('', 'W');
-- ----------sql1------------
SELECT T2.MOLECULE_ID FROM ATOM AS T1 INNER JOIN MOLECULE AS T2 ON T1.MOLECULE_ID = T2.MOLECULE_ID WHERE CAST(SUBSTR(T1.ATOM_ID, 7, 2) AS INTEGER) BETWEEN 21 AND 25 AND T2.LABEL = '+';
-- ----------sql2------------
-- krs
SELECT DISTINCT T2.MOLECULE_ID FROM ATOM AS T1 INNER JOIN MOLECULE AS T2 ON T1.MOLECULE_ID = T2.MOLECULE_ID WHERE SUBSTR(T1.ATOM_ID, -2) BETWEEN '21' AND '25' AND T2.LABEL = '+';
